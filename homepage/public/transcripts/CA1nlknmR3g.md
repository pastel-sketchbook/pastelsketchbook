---
type: source
videoId: CA1nlknmR3g
category: security
title: "Containing the Untrusted Agent"
views: 33
date: 2026-03-25T08:25:20Z
captured: 2026-04-14T09:44:34.076Z
---

# Containing the Untrusted Agent

> Category: [security](../../videos/security.md) · 33 views · Mar 25, 2026
> [Watch on YouTube](https://youtu.be/CA1nlknmR3g)

## Transcript

Welcome.  Today  we  are  going  to  explore  a critical  challenge  in  the  modern  AI landscape,  containing  the  untrusted agent.  As  we  increasingly  integrate large  language  models  into  our  systems, we  must  address  the  significant  security risk  of  serverside  request  forgery  or SSRF.  This  presentation  provides  a comprehensive  blueprint  for  defeating such  threats  by  utilizing  a  layered defense  in-depth  architecture  as illustrated  by  the  concentric  rings  in the  diagram.  Our  strategy  involves surrounding  the  untrusted  agent  with multiple  redundant  security  tiers.  By implementing  this  robust  framework,  we ensure  that  even  if  a  single  point  of failure  occurs,  the  core  system  remains protected  from  unauthorized  access  and malicious  exploitation.  Let's  begin  our deep  dive  into  these  defensive  layers. The  evolution  of  cyber  security  threats has  led  us  to  a  new  frontier  where  large language  models  act  as  active  network threats.  Traditionally,  serverside request  forgery  or  SSRF  has  relied  on the  manual  manipulation  of  vulnerable application  input  fields.  As  illustrated on  the  left,  an  attacker  might  input  a loop  back  address  into  a  URL  field  to trick  the  server  into  accessing restricted  internal  resources  such  as  a private  database.  However,  LLM  SSRF represents  a  fundamental  shift  in  this attack  factor.  Instead  of  direct  input manipulation,  this  exploit  leverages  the model's  inherent  capability  to  interact with  external  tools  and  APIs autonomously.  On  the  right,  we  see  a user  providing  a  simple  natural  language prompt  such  as  analyze  the  internal network  structure  for  me.  The  LLM  then translates  this  high-level  intent  into an  actionable  network  request  such  as  a get  request  to  an  internal  metadata  API without  the  user  ever  needing  to  specify a  malicious  URL.  This  transforms  the  LLM from  a  passive  interface  into  an  active agent  capable  of  probing  internal  server infrastructure  directly,  bypassing traditional  input-based  security controls.  This  slide  illustrates  the anatomy  of  an  LLM  serverside  request forgery  or  SSRF  breach,  broken  down  into four  critical  stages.  In  step  one,  the prompt,  an  attacker  submits  a  request disguised  as  a  legitimate  task,  such  as asking  the  model  to  summarize  content from  an  internal  metadata  URL.  Moving  to step  two,  the  execution,  the  LLM utilizes  its  integrated  web  fetching tools  to  act  on  this  request,  sending  an HTTP  get  request  to  that  specific internal  address.  Because  the  LLM  is operating  within  a  trusted  cloud environment,  we  reach  step  three,  the response.  The  internal  server,  assuming the  request  is  authorized,  blindly responds  with  sensitive  metadata  or secrets.  Finally,  in  step  four,  the xfiltration,  the  LLM  processes  this  raw response  and  inadvertently  includes  the exposed  secrets  in  its  final  reply  to the  attacker,  completing  the  breach. When  assessing  the  security  landscape for  large  language  models,  it  is critical  to  understand  the  primary targets  for  unauthorized  requests initiated  by  untrusted  agents.  There  are three  main  vectors  frequently  exploited. First,  attackers  target  cloud  metadata services  specifically  by  attempting  to reach  the  link  local  address 169254169254. This  allows  them  to  potentially  extract temporary  compute  credentials,  tenant IDs,  and  sensitive  network configurations.  Second,  untrusted  agents are  used  to  probe  internal microservices.  These containerto-container  APIs  often  lack robust  authentication  because  they reside  within  a  supposedly  trusted network.  For  example,  an  agent  might  be directed  to  hit  an  internal  user  API  to bypass  standard  security  layers.  Lastly, we  see  attacks  targeting  local  host environments  by  forcing  the  LLM  to  query localhost  or  127.0.0.1. Malicious  actors  can  gain  access  to administrative  panels  or  sidecar  proxies that  are  only  accessible  from  within  the local  host  environment.  Understanding these  targets  is  the  first  step  in implementing  proper  safeguards  against LLMbased  serverside  request  forgery. Securing  an  agent  requires  a comprehensive  defense  in-depth architecture.  To  effectively  defend against  LLMbased  serverside  request forgery,  we  must  adopt  the  mindset  of treating  the  model  as  an  untrusted  user already  operating  within  our  network. Because  a  single  point  of  failure  can lead  to  total  compromise,  our  strategy centers  on  building  concentric  rings  of structural  mitigation.  At  the  core,  we have  the  LLM  and  prompt  layer,  focusing on  the  immediate  interface  and  input sanitization.  Moving  outward,  the application  layer  provides  logic-  based boundaries  for  the  agents  actions.  The network  layer  follows,  enforcing  strict segmentation  to  prevent  unauthorized internal  access.  Finally,  the  cloud  and host  layer  offers  an  overarching perimeter  through  infrastructure  level isolation  and  identity  management.  By layering  these  defenses,  we  ensure  that a  breach  at  one  level  is  contained  and neutralized  before  it  can  impact  the entire  system.  Tool  narrowing  is  a critical  design  principle  for  securing LLM  integrated  applications.  The  core objective  is  to  replace  generic capabilities  with  tools  that  serve  a specific  explicit  intent.  On  the  left, we  see  a  naive  implementation,  a  generic web  fetching  tool  that  effectively  acts as  a  loaded  gun.  By  allowing  the  model to  control  the  destination  URL,  we introduce  a  significant  structural vulnerability  as  the  LLM  could  be coerced  into  accessing  sensitive internal  resources  or  malicious  external sites.  To  resolve  this,  we  must  adopt  a hardened  architecture  based  on  intent specific  tools.  Instead  of  a  broad  fetch function,  we  provide  a  scope  to  tool like  search  Wikipedia.  In  this  scenario, the  application  code  hard-codes  the  base URL  and  the  LLM  is  restricted  to providing  only  the  search  term.  By strictly  scoping  tools  to  their  intended business  purpose,  we  minimize  the  attack surface  and  ensure  the  model  operates within  safe  predefined  boundaries.  When dealing  with  highstakes  API  calls initiated  by  large  language  models,  it is  critical  to  implement  a  robust security  layer  through  enforced addation.  The  process  begins  when  an  LLM formulates  a  request  destined  for  an internal  web  hook.  To  prevent unauthorized  or  unintended  actions,  we introduce  a  mandatory  system  halt.  At this  juncture,  the  workflow  enters  an add  astation  step  where  the  intended destination  URL  and  the  complete  data payload  are  presented  clearly  to  the  end user  for  review.  This  provides  a  vital human  in  the  loop  verification mechanism.  If  the  user  approves  the request  after  careful  review,  it proceeds  to  execution.  However,  if  the user  identifies  any  discrepancy  and rejects  it,  the  request  is  immediately dropped,  ensuring  that  no  potentially harmful  or  incorrect  actions  are  taken without  explicit  human  authorization. Application  layer  defenses  require  a fundamental  shift  in  strategy.  Searching for  bad  inputs  is  a  losing  battle against  sophisticated  encoding  tricks. Instead,  the  application  wrapper  must enforce  strict  pre-flight  validation. Looking  at  the  traditional  approach  of blacklisting,  we  see  why  it  is inherently  flawed.  A  simple  logic  check such  as  blocking  the  string  169.254 within  a  URL  is  easily  circumvented. Attackers  can  bypass  these  filters  using DNS  rebinding,  octalike  key representations,  IPv6  formats,  or various  URL  encoding  techniques.  The ultimate  result  of  this  approach  is failure  as  the  attacker  successfully reaches  sensitive  internal  metadata.  In contrast,  strict  validation  offers  a secure  alternative.  This  methodology involves  protocol  enforcement  and  active IP  resolution  before  any  request  is executed.  It  effectively  neutralizes obfuscation  tactics  because  the  final resolve  destination  is  checked  against  a known  allow  list  rather  than  a  list  of forbidden  strings.  Consequently, unauthorized  routing  is  halted  at  the Python  or  node  level,  ensuring  that internal  resources  remain  protected.  The pre-flight  gatekeeper  sequence illustrates  a  robust  security architecture  for  managing  URLs  generated by  large  language  models.  This multi-layered  validation  process  ensures that  outgoing  requests  are  both  safe  and authorized.  The  sequence  initiates  once an  LLM  generates  a  URL.  The  initial validation  step  is  the  protocol  check, which  mandates  the  use  of  HTTPS.  By strictly  enforcing  the  standard,  we prevent  the  execution  of  potentially malicious  or  insecure  protocols  such  as file,  FTP,  and  standard  HTTP.  Following this,  the  domain  underos  an  allow  list check.  The  domain  is  compared  against  a predefined  list  of  trusted  sources  and any  unlisted  entries  are  automatically rejected.  The  final  security  layer  is the  IP  resolution  check.  This  involves resolving  the  domain  to  its  underlying IP  address  to  verify  it  does  not  reside within  private  CI  blocks,  including internal  network  ranges  or  cloudspecific metadata  endpoints.  Only  after  a  URL  has successfully  navigated  these  three critical  gates  does  the  system  permit the  authorized  request  to  proceed.  The redirect  bypass  vulnerability  is  a critical  security  concern  that  occurs when  an  application's  HTTP  client  is configured  to  automatically  follow redirects.  Attackers  can  exploit  this default  behavior  to  smuggle  malicious destinations  past  security  gatekeepers like  application  firewalls.  In  phase one,  an  initial  request  is  made  to  a seemingly  benign  URL  such  as trustedsite.com/redirect. Because  the  destination  appears trustworthy,  the  application  firewall allows  the  traffic  to  pass  through. Phase  two,  titled  shedding  the  mask, illustrates  what  happens  once  the request  is  inside  the  perimeter.  If  the HTTP  client  automatically  follows redirect  instructions,  it  will  proceed to  the  attacker's  true  destination without  further  inspection.  At  this point,  the  mask  of  the  trusted  site falls  away.  Finally,  in  phase  three,  the client  reaches  the  malicious destination,  which  in  this  example  is  a sensitive  internal  metadata  service. Since  the  firewall  was  bypassed  in  the initial  step,  the  internal  resource  is left  exposed  to  the  unauthorized request.  To  mitigate  this  risk, developers  must  explicitly  disable automatic  redirect  following  when  using HTTP  clients  like  requests  or  HTTPX.  By doing  so,  the  application  can  manually inspect  each  redirect  URL  before  a connection  is  established,  ensuring  that internal  or  malicious  destinations  are never  reached.  Layer  3  defense  focuses on  enforcing  a  default  deny  egress posture.  Because  application  level security  controls  can  potentially  be bypassed,  true  environmental  security requires  the  infrastructure  to inherently  reject  all  outbound  traffic from  LLM  pods  unless  it  is  explicitly authorized.  As  illustrated  in  the diagram,  an  LLM  agent  pod  is  contained within  a  restrictive  boundary.  Any outbound  requests,  whether  to  the  cloud metadata  service  at  169.254.169.254, 254.  The  public  internet  or  internal databases  are  blocked  at  the  network level.  This  is  technically  achieved through  a  network  policy  where  an  empty egress  list  translates  to  a  drop everything  rule.  By  implementing  this zero  trust  approach,  we  ensure  that  even a  compromised  pod  cannot  communicate with  unauthorized  external  or  internal resources.  To  implement  a  zero  trust security  model,  we  must  selectively allow  only  necessary  outbound  traffic from  our  applications.  Here  we  see  an LLM  agent  application  where  we  are poking  precise  holes  in  its  network perimeter  to  facilitate  essential communication.  The  first  requirement  is domain  resolution.  So  we  allow  egress  on UDP  port  53  specifically  to  the  cluster DNS.  The  second  requirement  is  reaching the  external  model  provider.  We  permit egress  on  TCP  port  443  but  restrict  it to  a  known  specific  external  IP  range  or CI  block.  It  is  critical  to  remember that  native  Kubernetes  network  policies operate  strictly  on  IP  addresses  and  not domain  names  which  necessitates  this CIbased  approach  for  securing  external communications.  When  securing  outgoing traffic,  we  can  implement  egress  routing through  either  layer  3  and  four  defenses or  more  advanced  layer  7  protections. Understanding  the  distinction  between these  two  approaches  is  key  to  building a  robust  security  posture  for  your workloads. Starting  with  the  routing  basis, Kubernetes  network  policies  operate  at the  network  and  transport  layers, meaning  they  function  primarily  based  on IP  addresses.  In  contrast,  ISTTO  service mesh  operates  at  the  application  layer, allowing  for  routing  based  on  fully qualified  domain  names  or  FQDNs. In  terms  of  configuration,  a  restrictive egress  stance  in  Kubernetes  is  achieved by  defining  empty  egress  arrays.  In ISTSTEO,  the  same  level  of  control  is enabled  through  the  registryon  mesh  mode setting,  which  blocks  all  traffic  that has  not  been  explicitly  defined  in  the service  registry.  Looking  at  optimal  use cases,  layer  3  and  four  network  policies are  best  for  static  internal  IPs  where the  network  landscape  is  predictable. For  more  complex  scenarios  involving dynamic  CDNbacked  external  APIs  where  IP addresses  change  frequently,  layer  7 defenses  like  those  provided  by  ISTO  are far  more  effective. Finally,  while  network  policies  are excellent  for  neutralizing  basic internal  network  mapping,  ISTTO  provides a  more  sophisticated  layer  of protection.  By  operating  at  the application  level,  it  neutralizes threats  such  as  dynamic  IP  rotations  and DNS  rebinding  bypasses  that  would otherwise  circumvent  simpler  layer  3  or four  rules.  Modern  web  architectures increasingly  rely  on  content  delivery networks  and  dynamic  infrastructures where  IP  addresses  are  constantly shifting.  Consequently,  traditional layer  3  IP  blocking  is  no  longer  an effective  security  measure  for restricting  outbound  traffic.  To  address this,  we  implement  layer  7  defense  using a  service  mesh  that  operates  at  the application  layer,  filtering  traffic based  on  exact  domain  names  rather  than volatile  IP  addresses.  On  the  left,  we see  the  default  allow  any  configuration. This  approach  lacks  granular  control allowing  unmonitored  connections  to traverse  the  network  freely  which significantly  increases  the  potential attack  surface.  In  contrast,  the registry  only  model  on  the  right represents  a  secure  zero  trust  posture. In  this  scenario,  all  outbound  traffic passes  through  a  service  mesh  proxy  that evaluates  requests  against  a  predefined registry  of  trusted  domains.  Connections to  authorized  servers  are  permitted while  any  traffic  destined  for unrecognized  domains  is  immediately blocked.  This  transition  to  domain-based filtering  provides  a  robust  and  reliable security  framework  for  managing  global outbound  traffic  in  modern  cloud environments.  In  this  slide,  we  examine the  ISTTO  intercept  sequence  which serves  as  a  critical  security  layer within  a  Kubernetes  pod  boundary. Consider  a  scenario  where  an  LLM container  attempts  to  make  an unauthorized  outbound  request  such  as  a call  to  a  cloud  provider's  metadata service  at  the  169.254 address  range.  Because  of  ISTTO's sidecar  architecture,  this  traffic  is automatically  intercepted  by  the  Envoy proxy.  Upon  interception,  the  proxy consults  its  service  entry  registry  to determine  if  the  destination  is  a  known and  permitted  external  service.  Finding no  match  for  this  sensitive  endpoint, the  proxy  instantly  drops  the  packet  and stamps  a  502  response  back  to  the  LLM container.  By  implementing  this  zero trust  posture,  unauthorized  outbound traffic  is  effectively  blocked  and  the attack  is  neutralized  at  the  immediate perimeter  of  the  pod.  The  gold  standard for  securing  large  language  model environments  involves  the  use  of dedicated  egress  gateways  to  maintain absolute  control.  All  outbound  LLM traffic  is  funneled  through  a  single heavily  monitored  proxy  server  before reaching  the  internet.  This  architecture establishes  a  centralized  choke  point enabling  comprehensive  deep  packet inspection  and  detailed  logging  of  all activity.  By  consolidating  traffic  in this  manner,  we  ensure  that  only strictly  validated  SNI  routed  traffic  is permitted  to  exit  the  network.  This provides  the  high  level  of  visibility and  security  required  to  protect sensitive  data  and  prevent  unauthorized external  communications.  SSRF  mitigation is  not  achieved  through  a  single  perfect line  of  code  but  through  a  unified ecosystem  of  structural  fallbacks.  If one  layer  fails,  the  next  catches  the threat.  This  ultimate  security  posture framework  establishes  defense  and  depth across  four  critical  levels.  At  the  LLM and  prompt  layer,  our  strategy  is  tool narrowing  where  we  hardcode  base  URLs and  limit  the  LLM  to  intentbased  queries to  prevent  unauthorized  navigation.  At the  application  layer,  we  implement  a validation  wrapper  that  enforces  domain allow  listing,  disables  redirects,  and performs  pre-flight  IP  resolution checks.  Moving  to  the  network  layer,  we focus  on  egress  control  by  utilizing Kubernetes  network  policies  at  layer  3 and  ISTTSTO's  registry  only  mode  at layer  7  to  automatically  drop  all unlisted  traffic.  Finally,  at  the  cloud and  host  layer,  we  anchor  our  security in  identity  and  AM  by  enforcing  zerorust compute  permissions  and  requiring internal  scoped  tokens  for  all  API  to API  calls.  We  ensure  that  every interaction  is  strictly  authenticated and  authorized.  This  multi-layered approach  creates  a  resilient  environment capable  of  neutralizing  sophisticated SSRF  threats.

---
*Captured on Apr 14, 2026.*