---
type: source
videoId: B81dHVYOYJA
category: development
title: "Inside the Typst Compiler Engine"
views: 35
date: 2026-05-11T00:02:22Z
captured: 2026-05-15T09:41:34.805Z
---

# Inside the Typst Compiler Engine

> Category: [development](../../videos/development.md) · 35 views · May 10, 2026
> [Watch on YouTube](https://youtu.be/B81dHVYOYJA)

## Transcript

Welcome  everyone.  Today  we  are  going  to take  a  closer  look  at  the  inner  workings of  the  tipped  compiler  engine.  This architectural  schematic  illustrates  the complete  life  cycle  of  a  document tracing  its  path  from  the  initial raw.tips  source  code  to  a  fully  realized accessible  output.  The  process  begins  on the  left  with  the  source  file  which  is transformed  into  an  intermediate representation  or  IR.  This  data  then flows  through  a  sophisticated multi-stage  layout  engine.  As  you  can see  from  the  gears  and  the  parser  and analyzer  section,  there  is  a  complex iterative  logic  at  play  that  ensures structural  integrity  and  visual precision.  Once  the  layout  is  finalized, the  engine  passes  the  information  to  the renderers.  This  stage  is  versatile, allowing  for  the  generation  of  various formats,  including  PDF,  SVG,  and  HTML. Finally,  the  pipeline  concludes  with  the accessible  output,  ensuring  that  the resulting  documents  are  not  only visually  polished,  but  also  meet  modern standards  for  accessibility. This  robust  architecture  is  precisely what  enables  TIPS  to  deliver  its  high performance  and  exceptional  type  setting quality.  TIPS  represents  a  significant advancement  in  document  preparation functioning  through  a  streamlined compilation  pipeline.  At  its  core,  the system  transforms  markup  based  source files  into  highquality  industry  standard outputs  including  PDF,  HTML,  SVG,  and PNG.  This  workflow  is  built  upon  three foundational  pillars.  First  is simplicity.  The  language  is  designed with  consistency  in  mind,  drastically lowering  the  barrier  to  entry  for  new users.  Second  is  composability,  where powerful  customization  is  achieved through  intuitive  hash  set  and  hash  show rules.  Finally,  performance  is  a  key differentiator.  The  system  utilizes incremental  compilation  powered  by  the underlying  KOMO  framework,  ensuring  that even  complex  documents  are  rendered  with exceptional  speed.  Interacting  with  the core  of  the  system  involves  navigating two  distinct  architectural  paths,  the CLI  and  IDE  environments.  On  the  left, CLI  integration  comprising  tips  CLI  and system  world  utilizes  a  batch  processing and  continuous  watch  execution  pattern. Its  resource  management  interacts directly  with  the  local  file  system  and the  host  operating  system  to  resolve file  ids  into  disk  paths.  For  command dispatch,  it  leverages  clap  to  handle subcomands  such  as  compile  and  watch while  managing  global  exit  codes  through thread  local  variables.  On  the  right,  we have  the  IDE  integration  via  tips  IDE and  IDE  world.  This  environment  is optimized  for  incremental  analysis  and interactive  querying  to  support real-time  feedback.  Resource  management is  achieved  by  implementing  packages  and files  methods  which  provide  the  compiler with  a  dynamic  registry  of  project assets.  To  bridge  the  gap  between specific  IDE  requirements  and  the general  compiler,  the  system  employs trait  upcasting  using  the  upcast  method to  convert  specialized  IDE  contexts  back into  the  core  tips/world  trait  object. The  core  compilation  journey  is  a multi-stage  process  where  the  primary transformation  logic  resides  within  the typest  crate  orchestrated  by  the  compile imp  function.  The  journey  moves  from  raw characters  to  precisely  positioned frames  through  four  critical  phases.  In phase  one  parsing,  the  system  performs lexical  analysis  to  transform  raw  input into  an  abstract  syntax  tree  or  a  phase two  focuses  on  evaluation.  Here  the  a  is interpreted  to  generate  actionable values  or  renderable  content  elements. Moving  to  phase  three  realization,  the compiler  applies  styling  logic.  This involves  processing  global  set  rules  and specific  show  rules  to  define  the  visual characteristics  of  the  elements.  The journey  concludes  with  phase  4  layout. This  final  stage  involves  arranging  all elements  into  pageionated  graphical frame  objects,  resulting  in  the  finished document  structure.  Phase  one  of  the compilation  process  focuses  on transforming  raw  input  text  into  a  typed abstract  syntax  tree.  This  transition begins  with  lexical  analysis  where  a dynamic  lexer  tokenizes  character streams  based  on  specific  contexts  such as  markup,  math,  or  code  modes.  Taking the  raw  text  example  equals  title,  the lexer  breaks  this  down  into  individual tokens  representing  the  equal  sign,  the whites  space,  and  the  identifier  title. Next,  these  tokens  are  processed  by  a memoized  recursive  descent  parser  to construct  a  concrete  syntax  tree  or  CST. The  resulting  untyped  syntax  nodes capture  every  element  of  the  source  text including  whites  space  and  operator precedence  providing  a  highfidelity representation  of  the  original  input. From  the  CST  we  move  into  typed  a generation.  In  this  stage  untyped  nodes are  lazily  cast  into  type-  safe  as  node representations.  As  shown  in  our  example the  initial  structure  is  transformed into  a  specialized  heading  node  which explicitly  defines  properties  like  depth and  content.  A  critical  feature  of  this phase  is  incremental  reparssing.  Rather than  rebuilding  the  entire  tree  after every  edit,  the  systems  reparse  function identifies  and  isolates  changes, updating  only  the  minimal  affected subree.  This  ensures  the  compilation process  remains  efficient  and  highly responsive  during  active  document editing.  Moving  into  phase  two,  we  focus on  evaluation  and  the  internal  workings of  the  virtual  machine.  In  this  stage,  a nodes  are  processed  by  the  VM  known  as tipped  eval  through  the  eval  trait. Every  node  is  responsible  for interpreting  itself  within  this environment.  The  VM  maintains  the  core execution  state,  managing  the  scope stack,  variable  bindings,  and  flow events  necessary  for  handling  loops.  To support  complex  language  features,  the captures  visitor  analyzes  the  a  to securely  bind  external  variables  for closures.  To  ensure  stability,  a  routing mechanism  monitors  call  stack  depth, effectively  preventing  infinite recursion.  The  VM  is  also  responsible for  resolving  module  imports  from  both local  and  remote  sources.  It  includes specific  safeguards  to  detect  and  block infinite  cyclic  import  loops.  Finally, the  evaluation  process  produces  two distinct  outputs.  Core  values  such  as numbers,  strings,  and  functions,  and content,  which  provides  the  essential building  blocks  for  the  final  document structure.  Phase  3  focuses  on  content realization  and  styling  where  complex content  blocks  and  their  associated style  chains  are  processed  through  a specialized  pipeline.  The  first  step show  rule  application  or  typed  realize is  where  userdefined  transformations  are materialized  into  concrete  elements. This  stage  is  essential  for  traceability as  it  generates  unique  source  locations for  every  materialized  element. Following  this  kindbased  transformations adapt  elements  to  their  specific context.  For  example,  the  engine  may unwrap  an  equation  element  if  it  is already  nested  within  a  math  block, ensuring  structural  consistency.  Next, grouping  rules  are  applied  to intelligently  organize  adjacent elements.  Text  segments  are  grouped  into textual  units,  while  inline  level elements  are  combined  into  PAR  blocks  to form  coherent  segments  for  the  layout engine.  The  final  stage  is  filtering  and optimization.  Here  the  engine streamlines  the  content  stream  by collapsing  redundant  space  element  nodes and  precisely  managing  paragraph  breaks. This  entire  process  transforms  raw content  into  a  flat  sequence  of  styled content  pairs  providing  an  optimized foundation  for  the  final  layout.  Phase  4 focuses  on  layout  orchestration  where the  typed  layout  engine  serves  as  the central  processing  unit.  Its  primary role  is  to  transform  realized  content into  a  paged  document  composed  of  fixed position  frame  items.  This transformation  is  achieved  through several  coordinated  subsystems.  The  flow layout  component  manages  the  primary document  flow  handling  critical  tasks such  as  column  distribution,  page breaks,  and  the  migration  of unterminated  elements  across  page boundaries.  In  tandem,  specialized formatting  provides  dedicated  modules  to handle  complex  spatial  arrangements, including  math  arrays,  responsive  grid cell  spanning,  and  birectional  inline text  shaping.  Parallel  to  these, geometric  construction  applies non-layout  impacting  style  modifications via  frame  modifiers.  It  is  responsible for  translating  abstract  shapes  into concrete  renderable  items.  As illustrated  in  the  diagram,  these processes  allow  various  content  types ranging  from  inline  text  stacks  to complex  tables  and  math  arrays  to  be structured  into  frames  which  are  then organized  into  the  final  paged  output. The  introspection  stabilization  loop addresses  a  fundamental  challenge  in document  processing.  Dynamic  elements like  tables  of  contents,  cross references,  and  page  numbers  require physical  layout  dimensions  that  have  not yet  been  generated.  Because  the  addition or  shifting  of  these  elements  can  change the  very  layout  they  depend  on.  A  single pass  approach  is  impossible.  To  solve this,  the  system  employs  an  iterative mechanism  centered  on  the  compile  im function.  This  function  repeatedly performs  a  full  document  layout, allowing  an  inspector  to  query  the  state of  TOC's  and  counters.  If  the  layout engine  detects  changes  between  passes, the  state  is  considered  unstable  and  the loop  iterates  again.  This  cycle continues  until  all  tracked  variables satisfy  the  convergence  checks  defined in  the  introspect  trait  at  which  point their  final  physical  positions  are locked.  This  process  is  protected  by  a max  iders  limit  to  ensure  safety  and system  stability  ultimately  leading  to  a fully  converged  and  accurate  document. Bundling  multifile  ecosystems  is  a critical  function  for  managing  complex documentation  projects.  As  shown  in  the diagram,  the  tipped  bundle  crate parallelizes  the  compilation  of  various subdocuments  such  as  separate  chapters and  raw  bite  assets  into  a  single unified  cohesive  output  structure.  A standout  feature  of  this  system  is  cross document  introspection.  By  utilizing  a bundle  introspector,  we  can  maintain  a unified  view  across  all  project  files. This  allows  for  seamless  native referencing  between  documents,  such  as citing  a  figure  from  chapter  1  within  a bibliography  in  chapter  5.  Finally, global  link  anchoring  ensures  that  every link  generated  is  unique  and  stable across  the  entire  project  footprint. This  creates  a  robust  navigation experience,  allowing  for  effortless movement  across  different  subdocuments within  the  final  bundled  output.  The developer  experience  is  central  to  our tooling  strategy,  as  shown  in  this  IDE tooling  map.  At  its  core,  the  system utilizes  several  key  modules  to streamline  the  writing  and  debugging process.  First,  we  have  autocomp completion  powered  by  complete.rs. This  feature  provides  contextaware suggestions  derived  from  evaluating  the abstract  syntax  tree,  allowing  the editor  to  suggest  specific  image extensions  or  variables  relevant  to  the current  scope.  Next,  jump.rs  enables jump  to  definition  through  birectional navigation.  This  allows  developers  to click  a  visual  frame  within  the  rendered PDF  preview  to  immediately  locate  its exact  span  in  the  source  code  or navigate  from  the  code  back  to  the corresponding  element  in  the  preview. For  deeper  insights,  the  analyze_expr module  performs  code  analysis.  It evaluates  identifiers  against  global scopes  to  determine  potential  runtime values  without  the  overhead  of  a  full document  recompilation.  Finally,  our  VS Code  integration  leverages  web  views  and code  lens  buttons  via  the  tips  test helper  to  facilitate  interactive  visual testing  and  reference  validation directly  within  the  editor  environment. This  slide  outlines  the  technical transition  from  internal  frames  to finalized  file  formats  within  the  types ecosystem.  We  will  examine  four  primary export  paths  PDF,  HTML,  SVG  and  PNG. Starting  with  PDF,  the  typed  PDF  crate leverages  the  Cor  library  for  efficient vector  assembly.  This  implementation goes  beyond  simple  conversion  by integrating  font  subsetting  to  optimize file  size,  handling  EXF  orientation  for images  automatically  and  calculating precise  metadata  to  meet  professional document  standards.  The  HTML  export  path utilizes  typed  HTML  to  transform internal  content  into  a  structured  HTML node  tree  to  maintain  visual  fidelity  on the  web.  It  employs  a  sophisticated  two pass  whites  space  protection  system  and dynamically  resolves  inline  CSS  displays ensuring  the  output  remains  faithful  to the  original  layout  intent.  For  vector graphics,  the  types  SVG  crate  uses  an XML  writer  for  structured  output.  A  key technical  highlight  is  the  aggressive dduplicator  logic.  By  identifying  and reusing  shared  assets  such  as  fonts, gradients,  and  clip  paths,  it significantly  minimizes  the  final  file size  while  maintaining  full  scalability. Finally,  for  rasterized  output,  typed render  relies  on  the  tiny  Skia  library. This  crate  features  an  intelligent routing  system  that  directs  standard glyphs  to  Pixlyph  for  rapid  rendering, while  complex  paths  are  drawn  manually to  ensure  maximum  visual  precision. Let's  take  a  deep  dive  into  accessible PDF  tagging,  specifically  focusing  on the  PDF-UA  standard.  The  fundamental objective  is  separating  layout  from meaning.  While  a  standard  PDF  is  often composed  of  fixed  geometric  frames,  an accessible  document  requires  these frames  to  be  translated  back  into  a logical  tag  tree  that  screen  readers  can accurately  parse.  On  the  left,  we  see the  visual  representation  of  a  table where  elements  are  defined  by  their spatial  positioning.  On  the  right,  the semantic  PDF-U  logical  groups  illustrate the  underlying  structure.  Here  the  data is  organized  into  a  hierarchy  mapping the  visual  cells  to  specific  tags  like TH  for  headers  and  TD  for  data  all nested  within  table  rows  and  the  parent table  element.  The  tree  builder  is responsible  for  this  resolution.  It  maps typed  content  into  group  count categories  and  ensures  strict  compliance by  wrapping  free  floating  marked  text sequences  in  a  tag-  span  with appropriate  block  placement. Furthermore,  we  implement  complex scoping  to  intelligently  handle conflicting  border  strokes  in  tables. This  ensures  accurate  header  scoping  and precise  semantic  bounding  box generation,  both  of  which  are  critical for  providing  a  coherent  and  reliable experience  for  users  of  assistive technologies.  The  foundation  toolkits encapsulated  in  the  types  kit  framework provide  the  essential  infrastructure  for our  environment.  This  framework  is organized  into  four  key  functional  slots that  streamline  the  development  process. Slot  one  focuses  on  font  management through  a  lazy  loading  font  book.  This system  unifies  font  assets  across embedded  data  system  directories  and userdefined  paths  providing  a  single efficient  interface  for  typography.  In slot  two  we  have  package  resolution. Here  system  packages  interact  directly with  the  typed  universe  registry  to handle  dependency  management.  These downloads  are  performed  atomically  to ensure  stability  and  consistent versioning  across  projects.  Slot  3 introduces  the  live  reload  server  which significantly  enhances  the  realtime development  workflow  by  leveraging  an HTTP  server  powered  by  serverent  events or  SSE.  The  framework  pushes  instant preview  updates  to  the  client  without requiring  manual  browser  refreshes. Lastly,  slot  4  comprises  file  watching and  caching.  This  component  uses  a  file store  to  cache  parsed  source  objects  for rapid  retrieval.  It  works  in  tandem  with an  event  batching  watcher  designed specifically  to  detect  file  changes efficiently  while  preventing  the performance  overhead  of  unnecessary recompilation  loops.  The  typed  ecosystem architecture  is  designed  for  modularity and  high-speed  performance.  On  the  left, we  see  the  primary  entry  points,  the command  line  interface,  and  various integrated  development  environments. These  inputs  are  processed  through  the typed  kit  utilities  before  entering  the core  processing  engine.  The  core pipeline  consists  of  four  distinct stages:  parsing,  evaluation, realization,  and  finally  layout.  A critical  feature  of  this  architecture  is the  introspection  return  path  which allows  data  from  the  layout  phase  to feed  back  into  the  parsing  and evaluation  stages  enabling  complex document  logic.  Once  the  layout  is finalized,  the  typed  bendle  facilitates the  distribution  of  content  into multiple  output  formats  including  PDF, HTML,  SVG  and  PNG  images.  The  underlying philosophy  as  highlighted  in  the synthesis  insight  is  that  types  achieves its  exceptional  incremental  performance by  avoiding  a  monolithic  structure. Instead,  it  maintains  a  rigorous decoupling  between  the  virtual  machine state,  the  page  document  layout,  and  the world  context.  By  ensuring  these elements  remain  distinct,  memorizable, and  asynchronously  updatable  across  the entire  pipeline,  the  system  can  provide a  highly  responsive  user  experience.

---
*Captured on May 15, 2026.*