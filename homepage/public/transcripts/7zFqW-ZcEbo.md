---
type: source
videoId: 7zFqW-ZcEbo
category: development
title: "The Rust Architect's Sketchbook"
views: 91
date: 2026-01-09T00:15:35Z
captured: 2026-04-14T09:43:10.851Z
---

# The Rust Architect's Sketchbook

> Category: [development](../../videos/development.md) · 91 views · Jan 8, 2026
> [Watch on YouTube](https://youtu.be/7zFqW-ZcEbo)

## Transcript

Welcome  everyone.  It  is  a  pleasure  to have  you  here  today  for  our  session titled  the  Rust  Architect  Sketchbook,  a blueprint  for  high  performance  systems. Over  the  next  hour,  we  will  delve  into the  core  principles  and  architectural patterns  that  make  Rust  a  premier  choice for  modern  systems  engineering.  We  will explore  how  to  leverage  its  unique memory  safety  and  concurrency  features to  build  applications  that  are  as  robust as  they  are  efficient.  Whether  you  are an  experienced  architect  or  an  aspiring systems  programmer,  this  presentation  is designed  to  provide  you  with  a  clear road  map  for  designing  high-scale high-performance  software  environments. Let  us  begin  our  exploration  of  these technical  foundations.  Building  not listing  is  our  philosophy  for  this session.  Rather  than  providing  a  dry catalog  of  libraries,  we  are  going  to explore  the  narrative  of  building  a modern  rest  service.  We  will  sketch  the system  layer  by  layer  starting  with  the foundational  engine  and  moving  toward  a productionready  application.  Our  journey begins  in  the  engine  room  where  we tackle  concurrency  and  the  core mechanics  that  drive  the  system.  From there  we  move  to  the  blueprint establishing  the  abstractions  that  serve as  a  universal  template  for  our services.  Once  the  foundation  is  set,  we build  the  framework  which  houses  the application  logic  itself.  Finally,  we apply  the  finishing  touches,  ensuring the  service  meets  all  readiness standards  for  a  production  environment. Each  tool  and  concept  will  be  introduced precisely  when  it  is  needed  in  the design  process,  providing  a  clear  and logical  path  through  the  architecture. To  build  truly  high  performance services,  we  must  master  the  art  of concurrency.  The  core  challenge  lies  in efficiently  managing  two  fundamentally different  types  of  work  within  our applications.  On  the  left,  we  have IObound  tasks.  These  involve  waiting  for network  requests,  reading  from  a  disk, or  communicating  with  a  database.  During these  operations,  the  CPU  is  mostly  idle while  waiting  on  external  resources.  In the  REST  ecosystem,  Tokyo  is  the standard  runtime  for  handling  this asynchronous  workload.  Conversely,  we have  CPUbound  tasks.  These  are  defined by  intensive  computations,  data processing,  or  complex  calculations where  the  CPU  is  fully  utilized.  Rayon is  the  primary  library  for  managing  this type  of  parallel  work  efficiently.  This brings  us  to  a  critical  architectural question.  How  can  these  two  distinct paradigms  cooperate  and  how  do  we  bridge the  gap  between  them  to  ensure  our service  runs  at  peak  efficiency?  Tokyo is  the  primary  IO  engine  and  runtime  for building  reliable  asynchronous applications.  It  is  specifically designed  to  handle  tens  of  thousands  of concurrent  connections  efficiently  using only  a  small  pool  of  OS  threads.  This efficiency  is  achieved  through  three  key pillars.  First  is  the  eventdriven  model which  pulls  non-blocking  IO  operations to  prevent  the  application  from  idling while  waiting  for  data.  Second  is  the use  of  lightweight  tasks  or  green threads  as  illustrated  in  the  diagram. Incoming  requests  are  managed  by  the Tokyo  which  multiplexes  these  tasks across  a  set  of  OS  threads.  This approach  allows  for  massive  concurrency with  minimal  resource  overhead.  Finally, Tokyo  is  a  battle  tested  foundation  for missionritical  systems  currently powering  large-scale  infrastructure  at industry  leaders  such  as  Discord  and Cloudflare.  Rayon  is  widely  considered the  workhorse  for  CPUbound  tasks  in Rust.  It  is  a  data  parallelism  library designed  to  make  the  transition  from sequential  to  parallel  computation  as seamless  as  possible.  By  leveraging  a thread  pool,  Rayon  efficiently  splits complex  tasks  to  run  concurrently  across available  processor  cores.  One  of  its greatest  strengths  is  its  simple  API.  As illustrated  in  the  code  comparison, converting  a  standard  sequential iterator  into  a  parallel  one  is  often  as straightforward  as  changing  a  call  from dotiter  topar. This  elegant  abstraction  allows  you  to unlock  significant  performance  gains with  minimal  changes  to  your  existing codebase.  Under  the  hood,  Rayon  employs a  sophisticated  work  stealing.  This mechanism  ensures  that  no  CPU  thread remains  idle.  If  a  thread  finishes  its assigned  tasks  early,  it  proactively steals  work  from  other  busier  threads. This  dynamic  load  balancing  maximizes CPU  utilization  and  overall  throughput. Furthermore,  because  Rayon  is  built directly  upon  Rust's  rigorous  ownership and  borrowing  model,  it  provides guaranteed  safety.  This  allows  you  to achieve  high  performance  parallelism while  remaining  entirely  free  from  data races,  making  it  both  a  powerful  and reliable  tool  for  modern  software development.  When  we  talk  about  bridging the  worlds  of  asynchronous  IO  and parallel  CPU  computation,  the  key  is separation  of  concerns,  not  the integration  of  thread  pools.  A  common mistake  or  antiattern  is  attempting  to run  Rayon's  workers  directly  on  Tokyo's blocking  pool.  This  often  leads  to thread  starvation  and  performance degradation.  Instead,  the  correct approach  is  to  treat  any  CPUbound  work as  a  blocking  task.  From  Tokyo's perspective,  Tokyo  provides  the  spawn blocking  function  for  exactly  this reason.  It  delegates  tasks  to  a  separate larger  thread  pool  specifically  designed for  blocking  operations.  This  prevents longrunning  computations  from  stalling the  main  async  runtime.  Looking  at  the code  sketch  provided,  we  can  see  how this  pattern  is  implemented  in  practice. We  define  an  asynchronous  function  run CPU  inensive  work  which  takes  a  compute closure.  Inside,  we  use  Tokyo.task.spawn blocking  to  bridge  from  the  async  world of  Tokyo  into  the  parallel  CPU  world  of Rayon.  Within  that  blocking  thread, Rayon  is  free  to  use  its  own  dedicated thread  pool  to  execute  the  computation. By  awaiting  the  result,  we  create  a clean  bridge  that  lets  each  runtime focus  on  its  primary  strength  without interfering  with  the  other.  The universal  blueprint  for  our  architecture is  the  tower  service  trait.  Before  we build  an  application,  we  must  establish a  common  language  for  all  its components.  And  tower  provides  this through  the  service  trait.  It  serves  as a  simple  yet  powerful  abstraction  for  an asynchronous  function  that  takes  a request  and  returns  a  future  resolving to  a  response.  This  concept  is fundamental  in  modern  REST  web frameworks.  In  both  Axom  and  Tonic, everything  from  an  individual  route handler  to  the  entire  server  is  a service.  As  we  examine  the  trait definition,  we  see  associated  types  for the  response  produced  on  success  and  the error  produced  on  failure.  These  are combined  into  a  future  type  that represents  the  eventual  output  of  the operation.  The  call  method  is  the  main entry  point  acting  as  the  mechanism  that converts  incoming  requests  into  futures. Furthermore,  the  trait  includes  Paul ready  to  handle  back  pressure,  ensuring the  service  is  prepared  to  accept  new work  before  the  call  method  is  invoked. Composing  services  in  tower  is  achieved through  the  use  of  tower.layer. Fundamentally,  a  layer  is  a  function that  wraps  an  existing  service  to augment  it  with  new  behavior.  This wrapping  pattern  is  exactly  how middleware  is  built  within  this ecosystem.  Because  layers  are  highly composable,  we  can  stack  multiple  layers to  construct  a  complete  processing pipeline  for  our  incoming  requests. Looking  at  the  diagram,  we  can  see  how this  architecture  works  in  practice.  At the  core,  we  have  our  base  service containing  the  primary  application logic.  This  service  is  wrapped  by  a  log layer  which  is  then  further  wrapped  by an  off  layer.  When  a  request  arrives,  it passes  through  the  layers  from  the outside  in,  first  through authentication,  then  through  logging before  finally  reaching  the  core application  logic.  The  response  then travels  back  out  through  these  same layers  in  reverse  order.  This  modular approach  allows  us  to  cleanly  separate crosscutting  concerns  from  our  primary business  logic.  Common  examples  of functionality  implemented  as  middleware layers  include  logging  and  tracing, authentication,  rate  limiting,  and response  compression.  By  using tower.layer,  layer,  we  can  easily  swap, add  or  remove  these  behaviors  without modifying  the  underlying  service implementation  itself.  Tonic  is  a high-performance  gRPC  framework  for  Rust built  directly  on  tower.  It  functions  by translating  abstract  service  blueprints into  concrete  implementations  designed for  high-speed  reliable  microservices. The  development  workflow  follows  three core  steps.  First,  we  define  the  API contract  using  protocol  buffers.  In  this example,  we  define  a  greeter  service with  a  say  hello  RPC  method  along  with its  corresponding  request  and  response messages.  This  creates  a  strictly  typed interface.  Second,  we  generate  the  rest code  using  a  build.rs  script.  We  call the  compile  protos  function  from  tonic build  to  process  our  protoiles.  This step  automates  the  creation  of  the underlying  communication  boilerplate. Finally,  we  implement  the  service.  We define  a  strruct  for  our  service  and  use the  async  trait  macro  to  provide  the specific  logic  for  each  RPC  method defined  in  our  contract.  This  approach ensures  that  our  implementation  remains synchronized  with  our  API  definition while  leveraging  Rust's  performance  and safety.  Axom  and  Tonic  are  powerful frameworks  for  HTTP  and  gRPC  services respectively.  Since  both  are  built  on top  of  the  tower  abstraction,  we  can seamlessly  integrate  them  into  a  single unified  server.  This  allows  us  to  handle both  types  of  traffic  through  a  single entry  point,  demonstrating  the  power  of a  shared  abstraction.  The  core  of  this integration  is  the  tower.steer.steer service.  This  service  allows  us  to  route incoming  requests  to  different  inner services  based  on  specific characteristics.  In  this  example,  we define  an  HTTP  service  using  Axom's router  and  a  gRPC  service  using  Tonic server  builder.  We  then  create  a  steer service  that  inspects  the  incoming requests  content  type  header.  If  the header  is  not  application  gRPC,  the request  is  routed  to  the  HTTP  service. Otherwise,  it  is  directed  to  the  gRPC service.  Finally,  we  use  axom.serve  to run  this  combined  service  on  a  single listener.  The  result  is  one  server  on one  port  handling  two  different protocols,  all  made  possible  by  the modularity  of  the  tower  ecosystem. Surday  is  the  de  facto  standard  for efficiently  and  generically  serializing and  deserializing  Rust  data  structures. It  serves  as  a  vital  bridge  between  your applications  internal  types  and  external data  formats  such  as  JSON,  YAML,  and ProtoBuff.  One  of  the  primary  reasons for  its  dominance  is  its  effortless integration.  By  simply  adding  the  derive attribute  for  serialize  and  deserialize to  your  strcts  as  seen  in  the  code example,  you  gain  the  ability  to  move data  in  and  out  of  your  system  with minimal  boilerplate.  This  simplicity does  not  come  at  the  cost  of  speed.  Sery leverages  REST's  zerocost  abstractions to  ensure  that  serialization  is  fast with  almost  no  runtime  overhead.  As  a foundational  crate  for  the  entire  Rust ecosystem,  Serde  handles  over  30  million downloads  per  month.  The  visual  workflow here  illustrates  the  core  concept.  By annotating  a  Rustruct,  we  enable  a seamless  birectional  conversion  between our  internal  data  types  and  a  standard JSON  string.  This  allows  for  clean type-esafe  data  handling  that  is  both robust  and  highly  performant.  Robust error  handling  is  non-negotiable  when building  production  grade  software. While  Rust's  result,  e  type  provides  a solid  foundation,  complex  projects  often require  more  sophisticated  tools  for management  and  reporting,  the  REST ecosystem  offers  two  complimentary libraries  to  address  these  needs.  This error  and  anyhow,  the  right  choice depends  entirely  on  your  context. Specifically,  whether  you  are  developing a  library  or  a  standalone  application. This  error  is  designed  for  creating specific  structured  error  types  making it  the  preferred  choice  for  library authors  who  want  to  provide  consumers with  clear  matchable  error  variants. Conversely,  Anyhow  is  built  for  highle applications  providing  a  simple  and flexible  way  to  handle  errors  where  ease of  use  and  detailed  context  are prioritized  over  granular  error matching.  Together,  these  tools  allow you  to  move  beyond  basic  error  handling and  into  a  robust  professional implementation.  In  the  Rust  ecosystem, the  choice  of  error  handling  library depends  heavily  on  whether  you  are developing  a  reusable  library  or  an enduser  application.  On  the  left,  we  see this  error  which  is  the  preferred  choice for  library  development.  It  enables  the creation  of  precise  custom  error  enums that  allow  callers  to  match  on  specific variants  programmatically  by  using procedural  macros.  This  error  simplifies the  implementation  of  the  error  trait, allowing  for  structured  fields  and custom  display  messages  as  seen  in  the data  error  at  ample.  This  results  in  a stable,  expressive  API  that  provides clear  guidance  to  anyone  consuming  your crate.  Conversely,  anyhow  is  designed specifically  for  applications  where  the primary  goal  is  simplicity  and  ease  of propagation.  Instead  of  defining  complex custom  types,  anyhow  utilizes  a  unified dynamic  error  type  that  can  wrap  any error  implementing  the  standard  error trait.  The  code  example  demonstrates  the use  of  the  context  method  which  allows developers  to  add  descriptive  layers  to an  error  as  it  bubbles  up  the  call  stack without  the  overhead  of  boilerplate code.  The  summary  table  at  the  bottom highlights  these  fundamental differences.  While  anyhow  provides  a dynamic  approach  suited  for  the  rapid development  cycles  of  applications,  this error  offers  the  static  structured approach  required  for  high-quality library  interfaces.  By  selecting  the tool  that  matches  your  project's  goal, whether  that  is  easy  propagation  or  a robust  matchable  API,  you  ensure  that your  error  handling  is  both  idiomatic and  effective.  To  achieve  deep visibility  into  what  our  services  are doing  in  production,  we  need  a  robust observability  framework.  The  tracing crate  provides  exactly  that,  allowing  us to  collect  structured  event-based diagnostic  information  through  three primary  components:  spans,  events,  and subscribers.  A  span  represents  a specific  period  of  time  with  a  context, such  as  the  duration  of  a  single request,  and  notably,  spans  can  be nested  to  represent  subtasks.  An  event represents  a  single  discrete  point  in time  within  a  span,  such  as  a  cache  hit or  an  error  occurrence.  The  subscriber is  the  component  that  collects  this trace  data  and  processes  it  whether  by formatting  it  as  logs  or  sending  it  to an  external  observability  platform.  To simplify  instrumentation,  we  use  the instrument  macro  as  shown  in  the  code example.  Applying  at  instrument  to  a function  automatically  creates  and enters  a  span  for  that  function's execution,  capturing  its  arguments  as part  of  the  context.  This  allows  us  to easily  track  metadata  such  as  a  user  ID throughout  the  life  cycle  of  a  request. A  modern  Rust  service  is  not  a  monolith but  a  well-designed  system  of cooperating  components  where  each library  plays  a  distinct  and  vital  role. At  the  core,  we  have  Tokyo  serving  as the  asynchronous  runtime  providing  the foundation  for  high  concurrency  tasks. When  we  encounter  CPU  inensive workloads,  we  leverage  rayon  for parallelism,  utilizing  a  spawn  blocking bridge  to  ensure  these  operations  do  not stall  our  async  executives.  Data handling  is  centralized  through  Sury which  handles  the  essential  tasks  of serialization  and  deserialization. This  feeds  into  our  service  abstraction layer  tower  which  utilizes  service  and layer  traits  to  manage  middleware  and request  flow.  Tower  provides  a consistent  interface  that  supports  both Axum  for  our  web  framework  needs  and tonic  for  gRPC  communication  where  tonic defines  the  API  contract.  Supporting  the entire  architecture  are  the  critical pillars  of  error  handling  and observability.  We  use  this  error  and anyhow  for  robust  error  propagation across  all  layers  ensuring  that  failures are  handled  gracefully  and informatively.  Finally,  the  tracing crate  captures  spans  and  events throughout  the  life  cycle  of  every request,  providing  the  observability necessary  to  maintain  a  performant  and reliable  application.

---
*Captured on Apr 14, 2026.*