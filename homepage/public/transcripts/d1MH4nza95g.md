---
type: source
videoId: d1MH4nza95g
category: development
title: "Tracing Asynchronous Rust"
views: 6
date: 2026-05-03T16:24:44Z
captured: 2026-05-03T21:52:00.217Z
---

# Tracing Asynchronous Rust

> Category: [development](../../videos/development.md) · 6 views · May 3, 2026
> [Watch on YouTube](https://youtu.be/d1MH4nza95g)

## Transcript

Welcome.  Today  we  are  diving  into  the architectural  foundations  of  the  Tokyo RS/tracing  ecosystem.  As  we  explore  the complexities  of  tracing  asynchronous Rust,  this  diagram  serves  as  our  map.  At the  very  top,  we  begin  with  the  root where  our  execution  context  originates. From  there,  you  can  see  the  hierarchical structure  of  spans  denoted  here  as tracing_pan.span. These  spans  encapsulate  distinct  logical blocks  of  work.  Within  these  spans, individual  events  or  tracing  event.event are  recorded.  Notice  how  the  flow accounts  for  the  unique  challenges  of asynchrony,  including  async  blocks  and await  points,  which  pause  and  resume execution  across  different  tasks.  All  of this  telemetry  data  eventually  converges at  the  bottom  towards  the  subscriber, which  is  responsible  for  collecting, filtering,  and  processing  these  signals. This  visual  representation  highlights how  the  tracing  library  maintains context  across  thread  boundaries  and task  yields,  providing  a  clear  view  into the  performance  and  behavior  of  our asynchronous  applications.  Asynchronous execution  fundamentally  shatters  the traditional  call  stack  model  that standard  logging  systems  rely  on.  In  a synchronous  environment,  as  shown  on  the left,  execution  follows  a  linear continuous  path  through  nested  stack frames  within  a  single  thread.  This allows  diagnostic  context  to  be  easily preserved  and  tracked.  However,  in  the asynchronous  world  of  Rust,  execution  is fragmented.  When  a  future  reaches  an await  point,  it  yields  control  back  to the  exeutor.  As  illustrated  on  the right,  when  that  task  resumes,  it  may  do so  on  an  entirely  different  thread, moving  from  thread  A  to  thread  B  and then  to  thread  C.  Because  traditional logging  context  is  typically  tied  to synchronous  call  stacks  or  thread  local storage,  that  vital  information  is effectively  abandoned  at  every  await point,  leaving  us  with  disconnected  and incomplete  log  traces.  The  architecture shown  here  represents  a  deliberately decoupled  data  pipeline  designed  for maximum  flexibility  and  modularity.  By strictly  separating  the  instrumentation of  application  code  from  the  consumption of  diagnostic  data,  the  framework ensures  that  components  interact  only through  standardized  traits.  This  allows developers  to  change  how  data  is processed  or  exported  without  ever needing  to  modify  the  core  application logic.  The  pipeline  consists  of  five distinct  stages.  It  begins  with generation  where  application  code  and macros  such  as  the  hashtag  instrument attribute  create  the  initial  diagnostic events.  Next  is  routing  where  the dispatch  interface  serves  as  the  conduit directing  these  events  to  the appropriate  destination.  From  there  we move  to  storage  managed  by  the  registry or  subscriber  which  maintains  the  state and  context  of  the  telemetry  data.  The fourth  stage  is  processing.  Here  we utilize  chained  layers  to  perform operations  like  filtering  and  formatting allowing  us  to  refine  the  data  stream  in real  time  based  on  specific  needs. Finally,  the  pipeline  concludes  with export.  This  is  where  the  processed  data is  sent  to  various  outputs  such  as  log appenders,  system  services  like  journal or  visualization  tools  like  flame graphs.  This  modular  approach  ensures that  the  entire  observability  stack remains  highly  maintainable  and adaptable  to  evolving  system requirements.  To  understand  how  we observe  complex  systems,  we  must  first define  the  atomic  units  of  diagnostic data.  The  foundational  building  block  is the  span  which  represents  a  unit  of  work with  a  distinct  start  and  end  time. spans  are  uniquely  tracked  by  an  ID  and are  capable  of  nesting  to  form  causal hierarchies.  For  instance,  a  database query  occurring  within  the  broader duration  of  a  web  request.  Within  these spans,  we  have  events.  An  event  is  an instantaneous  occurrence  much  like  a traditional  log  line  that  automatically inherits  the  context  of  the  currently active  span.  Finally,  both  spans  and events  are  supported  by  context.  This consists  of  static  metadata  such  as location  and  level  along  with  dynamic structured  fields  in  the  form  of  key value  data.  This  contextual  information ensures  that  every  piece  of  diagnostic data  is  rich,  searchable,  and interconnected.  The  instrumentation macro  provides  a  declarative  approach  to generating  spans  and  events  as illustrated  by  this  mapping  between  code and  the  resulting  diagnostic  data.  When we  decorate  a  function  like  process request  with  the  instrument  attribute, the  macro  automatically  derives  the span's  name  and  target  from  the  function signature.  By  default,  function arguments  such  as  wc  ID  are  captured  as structured  key  value  pairs  within  the span's  fields.  However,  for  sensitive data  like  the  secret  parameter,  we  can use  the  skip  argument  to  explicitly exclude  it  from  the  recorded  span. Additionally,  the  macro  can  be configured  to  automatically  emit  an event  whenever  the  function  returns  an error  variant  by  simply  adding  the  air flag.  This  declarative  style  ensures comprehensive  instrumentation  with minimal  boilerplate,  maintaining  a  clear separation  between  business  logic  and diagnostic  concerns.  Wrapping  futures allows  us  to  maintain  critical diagnostic  context  across  asynchronous boundaries.  The  instrument  trait facilitates  this  by  creating  an instrumented  wrapper  which  ensures  that the  associated  span  is  correctly  managed throughout  a  future's  life  cycle.  As  the diagram  demonstrates,  when  the  runtime pulls  the  instrumented  future,  the wrapper  automatically  calls  span.  If  the future  yields,  the  wrapper  calls span.exit  to  suspend  the  context.  Upon being  woken  and  pulled  again,  the  span is  re-entered.  This  automatic  entry  and exit  cycle  guarantees  that  span  context is  preserved  perfectly  regardless  of which  executive  thread  is  processing  the future  at  any  given  time.  This  slide details  how  the  system  routes  events  to the  currently  active  subscriber.  On  the left  hand  side  we  have  various  inputs specifically  raw  spans  raw  events  and unstructured  data  entering  a  funnel  that leads  to  the  central  dispatcher.  The dispatch  strct  serves  as  a  type  erased router.  A  significant  advantage  of  this architecture  is  that  code  never  directly references  a  subscriber.  Instead,  it emits  data  directly  to  the  dispatcher. This  creates  a  clean  layer  of abstraction  between  data  generation  and data  consumption.  From  there,  the dispatcher  manages  routing  based  on  two levels  of  priority.  The  global  default subscriber  configured  using  set  global default  handles  events  at  a  processwide level.  However,  the  system  also  supports a  thread  local  override  through  the width  default  function.  This  enables developers  to  temporarily  override routing  for  a  specific  thread's execution,  providing  the  flexibility needed  for  complex  multi-threaded environments.  The  registry  optimizes in-memory  span  storage  by  utilizing  a sharded  slab  pool,  which  ensures lock-free  concurrent  access  across multi-threaded  runtimes.  By  partitioning the  storage  into  multiple  shards,  we minimize  contention  and  allow  different threads  to  operate  independently.  Each span's  life  cycle  is  managed  through  an atomic  reference  count.  This  count  is incremented  upon  creation  and decremented  when  the  span  closes, enabling  immediate  and  efficient  memory reuse.  Within  the  span  data  structure itself,  we  include  an  extensions  type map.  This  is  a  thread  safe  type  erased any  map  that  allows  various  layers  to attach  heterogeneous  custom  data directly  to  the  span's  memory  block. This  design  provides  a  highly  flexible and  performant  way  to  associate additional  metadata  with  each  span throughout  its  execution.  This  diagram illustrates  the  process  of  composing behaviors  through  a  layered  stack.  By utilizing  the  layer  trait,  we  can  build highly  composable  data  pipelines  where specific  functionalities  are  chained together  using  the  subscriber  x.width method.  At  the  base  of  the  stack,  the inner  registry  handles  high  performance memory  and  maintains  state.  Above  it, multiple  outer  layers  such  as  the  filter layer,  format  layer,  and  error  layer operate  independently  to  filter,  format, and  export  data  without  interfering  with one  another.  As  shown  by  the  vertical progression,  various  life  cycle  hooks, including  on  new  span,  on  event,  and  on record,  pass  through  these  layers sequentially,  allowing  each  layer  to execute  its  specialized  logic  as  data flows  through  the  system.  Strategic filtering  is  essential  for  preventing runtime  bloat  by  managing  how  and  when diagnostic  data  is  collected.  This approach  is  structured  across  three distinct  phases.  At  compile  time,  we  use static  max  level  through  cargo  features to  entirely  strip  high  verbosity  tracing calls  from  the  binary.  This  results  in zero  runtime  overhead  for  those  levels as  the  code  literally  does  not  exist  in the  final  build.  At  the  global  runtime level,  N  filter  provides  a  powerful  way to  parse  Rust  log  directives.  It evaluates  static  rules  during  initial call  site  registration  and  tracks dynamic  spans  efficiently.  Furthermore, these  filters  can  be  dynamically reloaded  via  a  handle,  allowing  for real-time  adjustments  without  service interruption.  Finally,  per  layer filtering  allows  us  to  isolate  logic using  filter  X  combinators  like  and,  or, and  not.  This  enables  sophisticated routing  such  as  sending  error  logs  to  a persistent  file  while  keeping  the console  output  clean  with onlyformational  messages.  Together, these  layers  ensure  our  observability infrastructure  remains  performant  and precise.  Capturing  contextual  back traces  at  the  point  of  failure  is essential  for  modern  observability,  but it  often  introduces  significant performance  overhead.  The  tracing  error crate  addresses  this  by  decoupling formatting  from  capture,  allowing  us  to attach  a  complete  diagnostic  context  to Rust's  standard  error  types  with  minimal latency.  The  process  begins  during normal  execution.  As  spans  are  nested deeply,  the  error  layer  quietly  caches formatted  fields  within  the  registry's extensions.  By  performing  this formatting  incrementally  as  the  program runs,  we  avoid  a  heavy  processing  spike when  an  error  actually  occurs.  When  an error  does  occur  inside  the  deepest active  span,  the  instrument  result  trait wraps  the  error  into  a  traced  error which  triggers  spanrace.capture. This  capture  is  highly  efficient  because the  necessary  metadata  is  already  live in  the  registry.  In  the  final reconstruction  phase,  span  trace  queries the  error  layer  to  instantly  assemble  a formatted  hierarchical  view  of  the execution  path.  This  provides  developers with  a  clear  structured  back  trace without  the  need  to  reparse  fields  at the  moment  of  failure,  ensuring  that diagnostic  depth  does  not  come  at  the cost  of  system  performance.  To  ensure high  performance,  we  implement  a  logging architecture  designed  to  prevent  disk  IO latency  from  blocking  the  main application  thread.  As  the  main  thread generates  log  entries,  it  offloads  them into  an  in-memory  ring  buffer.  This non-blocking  queue  acts  as  a  high-speed intermediary,  allowing  the  application to  buffer  logs  and  yield  execution control  immediately.  Processing  is handled  by  a  dedicated  worker  thread that  asynchronously  consumes  data  from the  ring  buffer.  This  thread  manages  the rolling  file  appender  which  automates log  rotation  on  an  hourly  or  daily  basis and  handles  storage  pruning  to  optimize disk  usage.  To  ensure  data  integrity,  we incorporate  a  worker  guard.  This  RAI based  component  strictly  guarantees  that any  remaining  entries  in  the  buffer  are flushed  to  disk  upon  application termination  preventing  data  loss  during shutdown.  We  can  export  tracing  data  to a  variety  of  specialized  diagnostic tools  depending  on  the  specific  needs  of our  environment.  First  tracing  survey provides  a  wrapper  that  converts  spans and  events  into  machine  readable  JSON. This  is  particularly  useful  for distributed  telemetry  systems  that require  a  standardized  data  format  for processing  and  analysis.  Next,  for Linux-based  systems,  tracing  journal  D offers  a  highly  efficient  way  to transmit  structured  data  directly  to  the systemd  journal.  It  utilizes  mem  FD  and Unix  socket  file  descriptors  to  ensure minimal  overhead  while  maintaining  high performance.  Finally,  tracing  log  serves as  a  bridge  for  legacy  applications.  By acting  as  a  log  tracer,  it  captures standard  log  record  events  and  pulls them  directly  into  the  modern  tracing subscriber  pipeline.  This  allows  us  to unify  legacy  logs  with  structured tracing  spans  into  a  single  cohesive diagnostic  stream.  Generating  folded stack  traces  for  flame  graphs  is  a  core capability  of  tracing  flame,  which leverages  the  precise  duration  of  spans to  build  folded  stack  trace  files.  As the  diagram  illustrates,  tracing  data moves  through  a  hierarchy  of  nested spans  from  the  parent  span  down  to specific  child  and  grandchild  spans  into the  flame  layer.  By  utilizing  Flush Guard  to  maintain  data  integrity,  the system  exports  metrics  in  a  perfectly formatted  folded  stack  trace  format. This  intermediate  output  consists  of semicolon  delimited  function  paths followed  by  timing  values  which  are ideally  suited  for  visualization  tools like  Inferno  Flame  Graph.  Ultimately, this  process  provides  a  clear visualization  of  CPU  time,  allowing developers  to  efficiently  identify  and resolve  performance  bottlenecks. Building  a  foundation  for  observable systems  requires  a  structured  approach to  diagnostic  data  as  shown  in  the central  architecture.  This  process  moves from  generation  and  routing  into  storage and  processing  before  reaching  the  final export  stage.  It  is  essential  to recognize  that  Tokyo  RS/tracing is  not  a  standard  logging  library.  It  is a  decoupled  type-Safe  data  pipeline built  for  diagnostics.  By  strictly separating  declarative  instrumentation from  concurrent  state  management  and composable  processing  layers,  it preserves  critical  context  within  the most  complex  asynchronous  Rust environments.

---
*Captured on May 3, 2026.*