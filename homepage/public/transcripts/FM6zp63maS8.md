---
type: source
videoId: FM6zp63maS8
category: development
title: "hexcap: Elevating Terminal Packet Capture"
views: 50
date: 2026-04-20T22:17:31Z
captured: 2026-04-23T02:56:57.704Z
---

# hexcap: Elevating Terminal Packet Capture

> Category: [development](../../videos/development.md) · 50 views · Apr 20, 2026
> [Watch on YouTube](https://youtu.be/FM6zp63maS8)

## Transcript

Welcome.  Thank  you  all  for  joining  me today.  I  am  pleased  to  introduce  Hex Cap,  a  tool  designed  to  redefine  the experience  of  terminal-based  packet capture.  Our  mission  is  to  bridge  the gap  between  high-performance  data acquisition  and  intuitive  visual analysis.  By  blending  low-level  libpcap performance  with  structured  pastel schematic  TUI  analytics,  we  are elevating  the  capabilities  of  standard command-line  networking  utilities. As  you  can  see  in  this  conceptual schematic,  Hex  Cap  transforms  raw  data streams  into  a  structured  hexagonal grid.  This  provides  a  more  organized  and visual  method  for  interpreting  complex network  traffic  in  real-time. We  believe  this  combination  of  raw  power and  clear  aesthetic  visualization  offers a  superior  environment  for  deep  network forensics  and  monitoring.  When  we examine  the  landscape  of  terminal  packet inspection,  we  encounter  a  significant trade-off  between  speed  and  analytical depth.  On  the  left,  raw  command-line tools  like  TCP  dump  are  exceptionally fast  and  native  to  the  environment,  yet they  often  lack  visual  hierarchy.  In these  tools,  raw  bytes  tend  to  blend together  into  a  dense  wall  of  text, making  rapid  analysis  challenging.  On the  far  right,  heavy  GUI  applications such  as  Wireshark  provide  the  deep structure  and  analytical  capabilities required  for  complex  debugging.  However, the  use  of  these  tools  requires  a significant  context  switch,  forcing users  to  move  away  from  their  primary terminal-based  workflow.  The  Hex  Cap synthesis  represents  a  powerful  middle ground.  It  is  designed  to  bridge  this gap  by  retaining  the  speed  and  native environment  of  the  terminal,  while introducing  deep  visual  structure, comprehensive  protocol  decoding,  and stable  flow  states.  This  allows  for  a structured  analytical  experience  without ever  leaving  the  command  line.  Let's examine  the  state  management architecture  of  our  application,  which we  have  modeled  as  a  dual  thread  seesaw. This  design  balances  two  primary workloads  around  a  shared  central  state contained  within  an  Arc  Mutex  of  the On  the  left  side  of  the  seesaw,  we  have the  capture  thread.  This  runs  on  a standard  thread  and  handles  blocking  IO through  libpcap.  To  maintain  high performance  and  prevent  bottlenecks,  it only  holds  the  Mutex  lock  for  a  few nanoseconds,  just  long  enough  to  perform a  pushback  operation  of  new  data  into our  buffer.  On  the  right  side  is  the  UI render  loop,  driven  by  Ratatui  on  the main  thread.  It  operates  on  the  50-ms polling  interval.  While  it  holds  the lock  for  a  longer  duration, approximately  a  few  microseconds  to  read the  state,  draw  the  interface,  and unlock,  its  lower  frequency  ensures  that the  capture  thread  is  rarely  blocked.  In designing  this,  we  made  two  key architectural  decisions.  First,  we  opted not  to  use  Tokio,  since  we  are  dealing with  fundamentally  blocking  FFI  calls, an  asynchronous  executor  would  introduce unnecessary  overhead.  A  purely synchronous  producer-consumer  pattern provides  the  most  efficient  execution with  zero  runtime  cost.  Second,  we  chose not  to  use  channels  for  communication. Our  TUI  requires  random  access  to  the entire  packet  history  to  support features  like  scrolling  and  filtering. If  we  use  channels,  the  UI  thread  would be  forced  to  duplicate  that  storage  into its  own  local  buffer,  leading  to redundant  memory  usage.  By  using  a Mutex,  both  threads  can  interact  with the  same  source  of  truth  efficiently. Our  approach  to  graceful  thread  handoff during  interface  switching  is  designed to  eliminate  deadlocks  and  ensure  system stability.  The  workflow  initiates  when  a user  triggers  the  interface  picker, causing  to  query  available  devices through  the  pcap  library. To  begin  the  transition,  the  UI  thread issues  a  signal  by  flagging  an  atomic bool.  This  stop  signal  notifies  the current  capture  process  that  it  needs  to A  critical  step  in  this  architecture  is the  deadlock  prevention  mechanism.  By explicitly  dropping  the  Mutex  guard before  invoking  the  new  capture  handle start  method,  we  free  the  global application  state  and  prevent  resource This  allows  for  a  graceful  exit,  where the  capture  thread  observes  the  stop signal  within  its  100-ms  timeout  window and  terminates  cleanly. The  process  concludes  with  the  restart phase.  Once  the  old  thread  has  exited,  a fresh  capture  thread  is  initialized, securely  locking  onto  the  new  interface to  resume  data  collection  without interruption.  This  coordinated  handoff maintains  a  responsive  interface  while ensuring  thread-safe  state  transitions. The  process  filtering  matrix  illustrates the  fundamental  differences  between traditional  hardware  or  BPF  filters  and the  Hex  Cap  software  filter  approach. While  libpcap  is  limited  to understanding  ports,  Hex  Cap  bridges  the gap  to  application  awareness  by executing  an  lsof  command  every  5 seconds  to  map  packets  directly  to  PIDs and  process  names. When  we  examine  dynamic  port  handling, hardware  filters  fail  because  they become  stale  as  soon  as  sockets  open  or close.  Hex  Cap,  however,  automatically refreshes  and  tracks  these  ports  in real-time.  For  uninterrupted  capture, hardware  filters  are  hindered  by  the need  to  restart  capture  which  inevitably drops  buffer  data.  Because  Hex  Cap operates  at  the  application  layer,  it allows  for  a  continuous  capture  that never  needs  to  pause. Platform  support  is  another  key differentiator.  Hardware  filters  are often  heavily  kernel-dependent, frequently  tied  exclusively  to  Linux environments.  Hex  Cap's  lsof-based parsing  remains  robust  and  stable  across both  macOS  and  Linux.  Finally,  where hardware  filters  are  limited  to  strict IP  and  port  mapping,  Hex  Cap  provides the  flexibility  of  fuzzy  matching  for PIDs  and  process  names,  such  as  Chrome, making  it  a  significantly  more  powerful tool  for  complex  network  analysis. Bidirectional  flow  normalization  is  a process  where  raw  interleaved  packets are  algorithmically  grouped  into  stable, color-coded  connection  identities.  As illustrated,  traffic  moving  from  client A  to  server  B  and  the  return  traffic from  server  B  to  client  A  both  enter  the flow  key  sorting  stage.  By  applying  a specific  sort  function  to  the  protocol, addresses,  and  ports,  we  guarantee  that packets  from  both  directions  map  to  the exact  same  flow  identity.  This normalization  ensures  consistent tracking  regardless  of  the  direction  of packet  travel.  Following  this,  the round-robin  color  engine  assigns  one  of eight  distinct  pastel  colors  to  these connection  identities,  providing  clear visual  differentiation.  To  monitor real-time  performance,  a  30-sample  ring buffer  tracks  bytes  per  second bandwidth,  offering  a  detailed  view  of throughput  patterns  over  time. Universal  interoperability  is  achieved by  leveraging  the  classic  pcap  format, creating  a  seamless  ecosystem  for network  data  analysis.  The  process begins  with  live  capture,  where  packets are  streamed  directly  into  the application's  internal  ring  buffer  for immediate  processing.  From  here,  users can  export  data  using  a  simple  key command,  which  writes  packets  to  disk  in a  standard  format,  specifically  hex complete  with  the  classic  24-byte  global header  and  the  recognizable  XA1B2C3D4 magic  number. This  adherence  to  the  classic  standard ensures  native  interoperability  with industry-standard  external  tooling,  such as  Wireshark,  TCP  dump,  and  Scapy. Furthermore,  the  system  supports  an import  function  using  the  --read  flag, allowing  existing  capture  files  to  be loaded  back  into  the  ring  buffer  in paused  mode  for  detailed  retrospective While  modern  alternatives  like  pcapng exist,  the  choice  was  made  to  use classic  pcap  to  maximize  backwards compatibility  across  the  entire landscape  of  existing  network  tools. This  approach  avoids  the  unnecessary overhead  associated  with  multiple interface  name  resolution  blocks  found in  pcapng,  ensuring  the  most  efficient and  widely  compatible  workflow  possible. Lightweight  protocol  inspection  is designed  for  efficiency  and  speed  by focusing  on  the  most  critical  layers  of network  traffic.  Starting  at  the  top layer,  we  handle  Ethernet  and  IPv6 identification,  specifically  targeting EtherType  0x86DD. This  leads  into  the  middle  layer,  where we  prioritize  the  core  transport protocols,  TCP,  UDP,  and  ICMPv6. At  the  bottom  layer,  the  decoded  panel displays  the  protocol  panel,  providing immediate  access  to  essential  fields such  as  source  and  destination  ports, TCP  flags  like  SYN  and  ACK,  and  sequence numbers.  To  enhance  readability,  we implement  RFC  5952  mapping,  which formats  addresses  using  the  bracketed IPv6  to  port  notation  for  clear  visual distinction.  The  core  philosophy  of  this approach  is  targeted  decode  over dissection.  By  decoding  these  primary protocols  directly  into  a  uniform decoded  field  struct,  we  can  meet  95%  of terminal  inspection  requirements.  This method  deliberately  avoids  the significant  overhead  and  bloat  typically associated  with  traditional  plugin-based dissector  frameworks,  resulting  in  a much  leaner  and  faster  inspection process.  A  plain  hex  dump  is  often little  more  than  visual  noise,  making  it difficult  to  discern  meaningful  patterns at  a  glance.  To  solve  this,  the  hex  dump renderer  employs  categorisation  to create  immediate  structural  readability. We  [snorts]  conceptualize  this  process as  a  byte  prism.  As  raw  input  passes through  the  prism,  it  is  differentiated into  functional  categories:  null  bytes, printable  ASCII,  white  space,  high  value bytes  such  as  0xFF, and  other  miscellaneous  data. Each  category  is  assigned  a  specific identifier,  such  as  hex_null  or allowing  for  distinct  visual  rendering. To  complement  this  categorisation,  we apply  a  layout  rule  that  mirrors  the  hex IO  format.  16  bytes  are  rendered  per line,  split  into  two  8-byte  groups.  This combination  of  semantic  grouping  and structured  layout  ensures  that  the  data is  organized  for  rapid  scanning  and enhanced  comprehension.  The  theme engine,  titled  Adaptive  Visual Structure,  is  designed  to  provide consistent  high-fidelity  data visualization  across  diverse  terminal environments.  The  interface  relies  on  a semantic  coloring  system  that  separates structural  elements  from  raw  data. For  the  UI  Chrome,  we  define  specific roles  for  backgrounds,  accents,  and borders.  This  logic  extends  directly into  the  data  layer,  where  hex  values are  categorized,  distinguishing  ASCII characters  from  null  bytes  or  high-value hex  sequences  to  ensure  that  dense information  remains  parsable  at  a glance.  A  critical  design  choice  is  our use  of  explicit  color  control  rather than  relying  on  standard  terminal  reset commands.  By  avoiding  color  reset,  we prevent  terminal  defaults  from  breaking row  striping  and  hex  block  boundaries. This  explicit  mapping  of  foreground  and background  colors  is  essential  for maintaining  strict  visual  grids  and legibility  across  all  16  built-in themes.  To  streamline  the  initial  user experience,  we  have  implemented  ghosty auto  detection.  Upon  first  execution, the  engine  reads  your  terminal configuration  to  map  popular  color families  such  as  gruvbox,  solarized,  or flexoki  to  their  closest  built-in equivalent  automatically.  This  ensures the  application  integrates  seamlessly with  your  existing  workspace  preferences without  requiring  manual  setup.  Terminal navigation  accelerators  provide  a high-efficiency  interface  for  managing data  and  navigating  large  data  sets.  For data  extraction,  the  Y  and  U  keys  are used  to  capture  information  directly  to the  system  clipboard.  A  lowercase  Y copies  a  formatted  hex  dump  via utilities  like  PBcopy  or  xclip,  while  an uppercase  Y  copies  the  raw  hex  string, facilitating  instant  imports  into Wireshark  or  xxd  injections. Stable  bookmarks  are  managed  using  the semicolon  and  apostrophe  keys  to  toggle packet  marks.  These  marks  are  stored structurally  as  a  hash  set  of  64-bit packet  IDs  rather  than  simple  display indexes.  This  ensures  that  bookmarks remain  perfectly  stable  and  accurate even  when  the  data  view  is  subjected  to aggressive  filtering  or  sorting Finally,  Vim  native  scrolling  employs the  M  and  comma  keys  to  execute immediate  20-row  half-page  jumps.  This mechanic  vastly  accelerates  traversal through  massive  capture  buffers, allowing  users  to  navigate  through extensive  logs  with  the  speed  and precision  characteristic  of  Vim-based workflows.  The  hex  cap  architecture  is designed  to  achieve  high-performance synchronous  ingestion  while  remaining perfectly  isolated  from  a  rich On  the  ingestion  side,  data  flows  from the  network  interface  through  libpcap. A  process  filter  using  lsof,  PID,  and port  matching  is  applied  before  the traffic  enters  a  dedicated  capture  loop running  on  its  own  thread. At  the  core  of  the  system  is  a thread-safe  application  state  managed  by an  Arc  mutex. This  component  handles  flow  key normalization  and  storage,  serving  as the  bridge  between  the  high-speed capture  and  the  display  logic. The  rendering  pipeline  is  driven  by  a  UI thread  using  Ratatui  with  a  50-ms refresh  rate. This  thread  pulls  data  from  the  central storage  and  passes  it  through  a  series of  specialized  modules,  a  header decoder,  a  five-category  hex  renderer, and  an  18-slot  theme  engine.  This sequence  culminates  in  the  final terminal  rendering,  ensuring  that  the interface  remains  fluid  and  responsive regardless  of  incoming  packet  volume.

---
*Captured on Apr 22, 2026.*