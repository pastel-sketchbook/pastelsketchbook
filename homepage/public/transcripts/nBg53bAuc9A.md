---
type: source
videoId: nBg53bAuc9A
category: kubernetes
title: "Zero-Trust Database Credentials in Kubernetes"
views: 1
date: 2026-09-02T23:00:23Z
captured: 2026-09-03T01:19:43.238Z
---

# Zero-Trust Database Credentials in Kubernetes

> Category: [kubernetes](../../videos/kubernetes.md) · 1 views · Sep 2, 2026
> [Watch on YouTube](https://youtu.be/nBg53bAuc9A)

## Transcript

Welcome  everyone  to  today's  session. We're  here  to  delve  into  a  critical aspect  of  modern  cloud-native  security, zero-trust  database  credentials  in On  the  left,  you  see  a  visual representation  of  the  intricate  and secure  architecture  we'll  be  discussing, a  blueprint  for  safeguarding  sensitive This  presentation  will  provide  an architectural  blueprint  for  integrating CyberArk  CCP  with  TomEE,  focusing  on  how to  leverage  in-memory  volumes  and  mutual TLS  to  achieve  a  robust  zero-trust  model for  your  database  credentials  within  a Kubernetes  environment.  Let's  explore how  to  build  the  secure  foundation. Standard  credential  injection  techniques present  significant  vulnerabilities within  modern  application  deployments. Relying  on  direct  queries  to  credential stores  like  CyberArk  from  applications inside  environments  such  as  TomEE servers  or  embedding  sensitive information  directly  into  Kubernetes secrets  and  environment  variables  is fundamentally  flawed.  These  conventional approaches  leave  high-value  database credentials  susceptible  to  multiple attack  vectors  as  illustrated  here. Firstly,  sensitive  data  held  as Kubernetes  secrets  can  be  exposed through  API  leaks,  allowing  unauthorized access  to  what  should  be  protected information.  Secondly,  credentials stored  as  environment  variables  within  a container  are  vulnerable  to  process inspection.  An  attacker  gaining  access to  the  pod  could  simply  inspect  the {slash}  proc  {slash}  pid  {slash}  environ path  to  extract  these  keys.  Finally, credentials  residing  in  memory  or temporarily  written  to  disk  are  exposed through  memory  dumps.  Tools  can  target path  {slash}  dev  {slash}  mem  or  similar locations  to  extract  live  credentials directly  from  RAM  or  swap  space,  further compromising  database  connections.  The high-level  architecture  blueprint illustrates  our  secure  credential management  within  a  pod.  Within  this pod,  an  init  container  acting  as  the CyberArk  fetcher  is  responsible  for securely  retrieving  sensitive credentials.  It  leverages  mutual  TLS, mTLS,  and  a  service  account  SA  token  to authenticate  with  the  external  CyberArk Credential  Provider  CCP  via  an  HTTPS mTLS  connection.  Once  retrieved,  these credentials  are  then  written  to  a  shared in-memory  volume  specifically  configured as  a  tmpfs  empty  directory  mount  point at  var/run/secrets/db. This  ensures  the  credentials  are  never persisted  to  disk,  offering  a  highly secure  and  ephemeral  storage  mechanism. Subsequently,  our  primary  TommyE container,  which  hosts  the  application and  its  database  connection  pool,  reads these  credentials  directly  from  this secure  in-memory  file.  This  allows  the application  to  dynamically  configure  its JDBC  data  source  using  the  fetched credentials,  establishing  connections  to the  back-end  database  securely.  This multi-step  process  ensures  that  database credentials  are  dynamically  injected into  the  application  at  startup,  stored only  in  memory  for  their  life  cycle,  and never  directly  exposed  within  the application  image  or  configuration.  This slide  illustrates  how  we  secure  the transport  and  authentication  layer, specifically  for  the  CyberArk  Credential Provider  or  CCP. The  core  principle  here  is  that  the CyberArk  CCP  must  restrict  access  using strict  application  identification, allowing  the  Kubernetes  pod  to authenticate  itself  without  requiring  a bootstrap  password. We  achieve  this  through  several  robust methods.  First,  with  client certificates,  we  implement  mutual  TLS  or mTLS.  This  involves  injecting  a  client certificate  signed  by  an  internal  public key  infrastructure,  which  can  be  managed via  solutions  like  SPIFFE,  SPIRE, cert-manager,  or  standard  Kubernetes Secondly,  we  leverage  Kubernetes  JSON Web  Token  validation.  This  means  using the  pod's  projected  service  account token,  which  can  be  sent  either  as  a custom  HTTP  header  or  routed  through  a reverse  proxy  to  authenticate  the  pod  to the  CCP. Finally,  we  enforce  strict  network policies.  These  policies  limit  egress traffic,  ensuring  that  only  the designated  pod  or  namespace  can  reach the  CCP  rest  endpoint,  specifically  over HTTPS  on  port  443. >> Together,  these  mechanisms  provide  a comprehensive  and  secure  approach  to authentication  and  communication.  This slide  illustrates  an  annotated  retrieval sequence,  often  executed  within  an  init container  in  a  Kubernetes  environment. The  process  outlines  how  an  application can  securely  retrieve  its  credentials during  startup. The  first  step,  labeled  mTLS  handshake, involves  a  curl  command  to  interact  with a  credentials  management  service. This  curl  command  specifies  client  and CA  certificates,  client.crt,  client.key, ca.crt,  to  establish  a  mutual  TLS connection,  ensuring  both  the  client  and server  authenticate  each  other. The  request  targets  an  API  endpoint, querying  for  account  information  for  a specific  application  ID,  k8s-tommy-app, and  a  database  user,  db-prod-user, within  a  designated  safe,  db-safes. The  response  from  the  secure  API  call  is captured  in  the  response  variable. Next,  in  the  JSON  parsing  stage,  the  jq utility  is  used  to  extract  specific fields  from  the  JSON  response. We  parse  out  the  username  and  store  it in  the  user  variable,  and  similarly,  we extract  the  content,  which  typically holds  the  password  or  secret,  into  the pass  variable.  This  efficiently  isolates the  necessary  credential  components. Finally,  the  RAM  file  write  step securely  provisions  these  retrieved credentials  for  the  main  application.  A cat  command,  utilizing  a  here  document, writes  the  extracted  username  and password  into  a  properties  file  named creds.properties,  located  in This  location  often  refers  to  a RAM-backed  file  system  or  a  temporary secure  volume  accessible  only  to  the  the ensuring  that  sensitive  data  is  not persisted  on  disk  longer  than  necessary. The  application  can  then  read  these properties  to  connect  to  its  database. >> Eliminating  disk  exposure  for  critical data  is  achieved  through  the  strategic use  of  tmpfs. This  in-memory  concept  diagram illustrates  our  approach  within  a Kubernetes  node  environment,  where  the traditional  storage  on  a  hard  disk  drive is  bypassed  for  sensitive  operations. Retrieved  credentials  must  never  be written  to  persistent  disk.  Instead,  by utilizing  a  mounted  empty  dir  volume type  backed  exclusively  by  the  node's RAM,  we  ensure  that  the  credential  file is  stored  entirely  in  volatile  memory. This  design  provides  a  crucial  security benefit.  The  sensitive  credential  file vanishes  instantly  and  irrevocably  if the  pod  terminates,  crashes,  or  is evicted,  preventing  any  residual  data from  lingering  on  disk. >> To  provision  a  RAM-backed  volume securely,  we  configure  both  its definition  and  permissions.  The  YAML volume  manifest  outlines  an  empty  dir named  shared  creds,  specifying  memory  as its  medium,  and  setting  a  size  limit  of 10  MB  for  temporary  in-memory  storage. Complementing  this,  strict  bash permissions  are  applied.  chmod  600 ensures  that  only  the  file's  owner  can read  var  run  secrets  dbcreds.properties, preventing  unauthorized  access. Furthermore,  chown  1001:1001 assigns  ownership  to  a  non-root  user  and group,  reinforcing  the  critical  practice of  running  all  containers  with  a non-root  UID  such  as  1001.  This  approach guarantees  that  temporary  credentials residing  in  memory  are  accessed exclusively  by  their  designated  non-root owner.  The  core  of  achieving  zero cleartext  consumption  in  Tommy  revolves around  handling  sensitive  credentials dynamically  and  securely,  leveraging  a shared  in-memory  volume. Credentials  are  provided  via  this  shared in-memory  volume,  ensuring  they  reside securely  in  RAM. From  there,  Tommy  is  meticulously configured  to  inject  these  credentials directly  into  its  connection  pool  at runtime.  This  injection  mechanism specifically  targets  files  such  as or  tommy.xml  without  ever  writing  the actual  credentials  to  disk.  Crucially, this  design  completely  bypasses  the  need to  declare  plain  text  passwords  in Kubernetes  config  maps  or  any  other deployment  manifests,  significantly enhancing  the  overall  security  posture. This  Tommy  EE  consumption  strategy matrix  presents  two  distinct  options  for managing  sensitive  data  consumption within  Tommy  EE  applications.  Option one,  system  loading,  utilizes  property replacement  directly  from  external properties  files.  This  method  offers  low complexity  as  it's  a  native  Tommy  EE feature,  requiring  only  a  startup argument  such  as  -D =  var.run.secrets.db.creds.properties. However,  it  introduces  a  moderate  clear text  risk  in  the  main  configuration where  variables  like  password  =  db password  would  be  visible,  indicating the  password  source.  Option  two,  the custom  password  cipher,  involves implementing  a  custom  password  cipher Java  class  to  read  the  sensitive information  from  a  file.  This  approach carries  a  medium  level  of  complexity  as it  requires  writing  and  deploying  custom Java  code,  for  example, The  significant  benefit  here  is  zero clear  text  risk  in  the  configuration itself.  The  config  merely  points  to  the file  path,  such  as  password  = without  exposing  the  actual  password value  directly.  The  credential  rotation challenge  outlines  a  significant  hurdle in  our  current  system,  specifically  how we  manage  database  credentials.  While the  init  container  pattern  serves  us well  for  initial  pod  startup,  a  critical issue  arises  when  a  system  such  as CyberArk  performs  a  database  password rotation  while  applications  like  Tommy  E are  actively  running.  As  depicted  by  the visual  flow  ending  in  a  disruption,  this change  inevitably  causes  active  database connections  to  fail  leading  to  service interruptions.  Therefore,  our architecture  must  adapt  to  support dynamic  credential  rotation.  This adaptation  requires  applications  to seamlessly  refresh  their  database connections  with  new  credentials eliminating  the  need  for  any  manual  pod restarts,  which  is  key  for  maintaining continuous  operation  and  enhanced security.  Examining  the  architectural modes  for  credential  life  cycles,  we primarily  consider  two  distinct approaches,  the  init  container  mode  and the  sidecar  container  mode. Let's  first  look  at  the  init  container mode.  It's  run  frequency  is straightforward.  It  executes  only  once, specifically  at  pod  boot.  This  design inherently  leads  to  low  complexity. However,  its  method  for  rotation handling  is  a  significant  point  to It  necessitates  a  full  pod  restart  to fetch  any  new  CyberArk  credentials.  In contrast,  the  sidecar  container  mode operates  quite  differently.  Its  run frequency  is  continuous,  running persistently  in  the  background.  This continuous  operation  contributes  to  a higher  level  of  complexity  compared  to the  init  container  mode.  Where  it  truly excels  is  in  rotation  handling.  A sidecar  container  periodically  refreshes credentials  within  the directory,  thereby  fully  supporting dynamic  runtime  rotation  without requiring  the pod  restart.  This  diagram illustrates  the  sidecar  rotation  loop,  a crucial  process  for  managing  credential rotation  and  ensuring  secure  database The  loop  begins  when  the  sidecar  fetches new  credentials,  initiating  the  rotation These  newly  fetched  credentials  are  then securely  written  to  a  RAM  volume,  making them  available  to  the  application. Next,  as  the  application  attempts  to  use existing,  potentially  stale  connections, the  TomEE  DBCP  database  connection  pool catches  an  SQL  authentication  exception. This  exception  signals  that  the  current credentials  are  no  longer  valid. Critically,  this  step  requires  test  on borrow  equals  true  and  validation  query equals  select  one  to  be  configured  in the  connection  pool,  ensuring  that  each borrowed  connection  is  validated  before use.  Upon  catching  the  authentication exception,  TomEE  intelligently  prunes any  stale  connections  from  its  pool  and pulls  the  fresh,  updated  credentials directly  from  the  RAM  volume.  This action  allows  the  application  to reestablish  connections  with  the  newly rotated  credentials,  seamlessly continuing  its  operations  with  updated security.  This  continuous  loop  ensures that  credentials  are  regularly  rotated and  applications  remain  connected  with valid,  secure  access.  The  fully fortified  architecture  presented  here details  how  sensitive  credentials  are managed  within  a  CyberArk  enabled  TomEE application  pod.  This  design  integrates several  layers  of  security  to  protect critical  data. Within  the  Kubernetes  pod,  an  init container,  designated  as  a  credential fetcher,  establishes  communication  with the  CyberArk  Central  Credential  Provider or  CCP  server.  This  interaction  is fortified  by  mutual  TLS  authentication, mandating  both  a  pod  client  certificate and  a  CyberArk  app  ID  for  robust identity  verification. The  transport  layer  further  enhances security  by  forcing  HTTPS  over  TLS  1.3, ensuring  highly  encrypted  data  exchange between  the  pod  and  the  CCP. Critically,  fetched  credentials  are never  persisted  to  disk.  Instead,  they reside  in  a  shared  in-memory  volume, specifically  a  RAM-backed  tmpfs configured  with  emptyDir.medium  equals This  volume,  mounted  at guarantees  zero  disk  writes,  thereby eliminating  the  risk  of  persistent  data Access  to  these  secret  files  is rigorously  controlled  through  chown  1001 and  chmod  600  permissions,  and  all container  execution  within  the  pod operates  as  a  non-root  user. Finally,  at  the  network  perimeter,  a Kubernetes  network  policy  is  enforced  to strictly  limit  the  pod's  outbound connections  or  egress  only  to  the designated  IP  addresses  of  the  CCP  and any  required  database  servers. This  comprehensive  multi-layered approach  ensures  that  sensitive credentials  are  acquired,  stored,  and utilized  with  the  highest  level  of security  throughout  their  operational life  cycle  within  the  application  pod.

---
*Captured on Sep 2, 2026.*