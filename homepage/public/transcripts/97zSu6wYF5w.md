---
type: source
videoId: 97zSu6wYF5w
category: development
title: "Cross-Runtime Development with Rust and napi-rs"
views: 23
date: 2026-07-10T23:00:35Z
captured: 2026-07-12T17:59:45.000Z
---

# Cross-Runtime Development with Rust and napi-rs

> Category: [development](../../videos/development.md) · 23 views · Jul 10, 2026
> [Watch on YouTube](https://youtu.be/97zSu6wYF5w)

## Transcript

Welcome  everyone.  I'm  excited  to  dive into  a  fascinating  area  of  modern development.  Today,  we'll  be  exploring cross-runtime  development  with  Rust  and Napi-RS.  This  presentation  will  focus  on the  practical  aspects  of  building high-performance  packages  for  Node.js and  Bun.  We'll  uncover  how  Rust,  coupled with  the  power  of  Napi-RS,  enables  us  to create  efficient  and  robust  modules  that seamlessly  integrate  across  different JavaScript  runtimes,  offering significant  performance  gains  and broader  compatibility.  The  core challenge  we  address  is  efficiently managing  cross-runtime  compatibility, and  Napi-RS  provides  the  definitive solution  for  hybrid  packages.  Our approach  begins  with  a  Rust  core  engine, which  is  transformed  into  a  universal .node  binary.  This  single,  optimized binary  can  then  be  seamlessly  consumed by  various  JavaScript  runtimes, specifically  Node.js  and  Bun,  as illustrated.  This  method  offers  three distinct  advantages.  First,  it  delivers native  Node  API  support  directly  within Bun.  Second,  it  accomplishes  this without  introducing  any  WebAssembly  or WASM  performance  tradeoffs.  Finally,  it eliminates  the  need  for  runtime-specific conditional  logic,  significantly simplifying  development  and  maintenance. This  slide  introduces  the  hybrid  package architecture,  a  design  focused  on achieving  a  clean  separation  of concerns,  specifically  for  a compute-core  project.  The  fundamental idea  is  that  low-level  native  logic  can operate  entirely  independently  from  its high-level  distribution  layer.  To illustrate  this,  the  domain  map  breaks down  the  two  core  components.  On  the left,  we  have  the  Rust  core,  which encapsulates  the  native  logic.  This includes  essential  Rust  project  files like  cargo.toml  for  dependencies  and metadata,  build.rs  for  build  script execution,  and  src/lib.rs containing  the  core  library implementation.  On  the  right,  we  see  the TypeScript  wrapper,  which  handles  the high-level  distribution  and  provides  an interface  to  the  Rust  core. Key  files  here  include  package.json  for managing  NPM  packages  and  project metadata,  tsconfig.json for  TypeScript  compiler  configuration, and  ts/src/index.ts as  the  entry  point  for  the  TypeScript wrapper  code  that  interacts  with  the compiled  Rust  module.  Phase  one  of  our development  focuses  on  configuring  the core  Rust  components,  laying  down  the fundamental  toolkit  for  our  project. Looking  at  the  cargo.toml  file,  we define  our  library  crate.  Crucially,  we set  crate  type  to  cdylib,  indicating that  Rust  will  compile  this  into  a C-compatible  dynamic  library. This  is  essential  for  interoperability with  other  languages  or  environments that  can  link  against  C  ABIs. We  also  name  this  core  component  compute Next,  the  build.rs  file  plays  a  vital role.  It  includes  extern  crate napi-build,  signifying  that  we're leveraging  the  napi-build  crate.  Within its  main  function,  napi-build.setup  is This  function  from  the  napi-build  crate handles  the  necessary  configurations  to create  Node.js  N  API  bindings,  allowing our  Rust  compute  core  to  be  seamlessly integrated  and  called  from  JavaScript applications.  For  phase  one,  we  focus  on the  core  performance  logic  written  in Rust,  illustrated  here  in  src/lib.rs. Our  process  data  function  demonstrates how  straightforward  it  is  to  expose high-performance  Rust  logic  to JavaScript.  The  @napi  macro,  applied directly  above  the  function,  is  the  key component.  It  automatically  handles  the seamless  type  mapping  between  JavaScript and  Rust,  allowing  us  to  pass  a  string from  JS  and  receive  an  i32  back  without complex  manual  bindings.  This explanatory  visual  details  how  a  macro acts  as  an  automated  translator, effectively  eliminating  manual  binding overhead,  and  ensuring  precise  type safety  across  language  boundaries.  As illustrated  in  the  bridge  diagram  on  the left,  we  observe  common  Rust  data  types, an  i32  integer,  a  string,  and  a  generic Vec  of  T  for  vectors.  The  central  HashN API  macro  serves  as  the  intelligent bridge,  automatically  translating  these Rust  types  into  their  appropriate JavaScript  counter  parts  on  the  right. An  i32  in  Rust  seamlessly  becomes  a number  in  JavaScript.  A  Rust  string  maps directly  to  a  JavaScript  string,  and  a Rust  Vec  of  T  is  robustly  converted  into a  JavaScript  array.  This  process underscores  the  macro  magic  of  achieving reliable  interoperability  with  minimal developer  effort.  Phase  two  outlines  the TypeScript  distribution  wrapper.  In  the package.json  file,  we  configure  the build  script  to  execute  Napi  build  - platform  and  TSC.  This  command  bundles the  native  library,  generates  automatic bindings,  and  then  compiles  the TypeScript  source  code.  Correspondingly, our  tsconfig.json contains  essential  compiler  options  with outDir  set  to  /dist.  This  precisely targets  a  unified  directory  for  all distribution  output,  and  declaration equals  true  ensures  type  declaration files  are  also  generated.  Phase  two focuses  on  building  the  idiomatic TypeScript  layer,  which  acts  as  a  bridge to  our  native  compute  core. Here,  we  see  an  example  from First,  we  directly  import  the  process data  function  from  the  auto-generated native  binary,  as  indicated  by  point This  process  data  function  is  our low-level  binding. Point  two  highlights  the  run  compute function,  which  serves  as  our higher-level  developer-friendly  API.  It provides  input  validation,  throwing  an error  for  an  empty  string,  and  sets  a default  parameter  for  the  input. Finally,  as  shown  by  point  three,  this run  compute  function  wraps  those low-level  native  bindings  in  a  way  that is  robust,  validated,  and  easy  for developers  to  use.  This  slide illustrates  the  compilation  flowchart for  our  unified  build  pipeline.  The process  initiates  with  the  familiar  npm run  build  command,  which  then  invokes the  @native-api-rs/cli. This  central  command  line  interface orchestrates  the  build,  directly producing  .node  files  and  TypeScript declaration  files,  .d.ts. Concurrently,  @native-api-rs/cli also  triggers  tsc,  the  TypeScript compiler,  which  transpiles  our TypeScript  source  into  executable  .js Ultimately,  all  these  compiled artifacts,  the  .node  binaries,  the generated  .js  files,  and  the  .d.ts  type declarations,  are  consolidated  into  the dist/  directory,  forming  our  complete build  output.  The  anatomy  of  the  payload illustrates  how  we  deliver  a  fully self-contained  output,  marrying  native execution  speed  with  an  idiomatic TypeScript  developer  experience.  Our dist  directory,  the  output  of  this process,  is  structured  into  three essential  components.  It  includes  the target  architecture  .node  binary, optimized  for  native  execution.  You'll also  find  the  clean  compiled  .js  files, representing  the  core  JavaScript  logic. Crucially  for  TypeScript  users,  the  dist folder  also  contains  the  .d.ts declaration  maps,  providing comprehensive  type  information  for seamless  integration  and  development. This  slide  illustrates  the  crucial concept  of  cross-runtime  parity  between Node.js  and  Bun.  A  key  design  principle is  that  Bun  mirrors  the  Node  API specification,  all  the  way  down  to  the binary  application  binary  interface,  or ABI  layer,  which  results  in  completely uniform  consumption  for  developers. Examining  the  execution  engine  matrix, we  see  that  both  Node.js  and  Bun  provide native  core  support  for  the  Node  API specification.  Significantly,  neither runtime  imposes  any  compilation requirement  as  Bun  functions  as  a  pure ABI  mirror  of  Node.js. This  means  that  invoking  your  JavaScript file  is  simply  node  index.js  for  Node.js and  bun  index.js  when  executing  with In  both  cases,  the  resulting  output  is 100%  binary  execution,  ensuring consistent  behavior  and  seamless interoperability  across  these  two environments.  Our  verification  and  proof of  concept  directly  demonstrate identical  outputs  and  seamless  execution across  engines  without  a  single  line  of runtime  specific  conditional  logic.  On the  left,  you  see  the  execution  of index.js  using  Node.js.  The  terminal output  clearly  indicates  the  compute core  executing  a  native  Rust  module, completing  the  operation  successfully  in 0.1  milliseconds.  Crucially,  on  the right,  we  show  the  exact  same  index.js file  being  executed  by  Bun.  Observe  the identical  output.  The  compute  core  again executes  the  native  Rust  module  and  the operation  is  successful  with  the  same execution  time  of  0.1  milliseconds. This  side-by-side  comparison  strongly validates  our  ability  to  deliver consistent  high-performance  results irrespective  of  the  underlying JavaScript  runtime,  proving  the portability  and  efficiency  of  our architectural  approach.  This architecture  arc  illustrates  how  Napi-RS facilitates  the  creation  of  native modules  for  JavaScript  runtimes. Developers  can  compile  a  source  payload, indicated  here  by  dist/payload and  the  @napi  annotation,  into  a  .node binary.  This  module  is  then  seamlessly integrated  and  executable  within  both Node.js  and  Bun  environments. This  approach  offers  three  key  benefits. First,  for  native  performance,  Napi-RS enables  direct  C  ABI  execution, completely  bypassing  the  performance overhead  often  associated  with WebAssembly.  This  ensures  optimal  speed and  efficiency  for  your  native  code. Second,  we  provide  unified  tooling through  a  single  automated  pipeline  that takes  your  source  code  all  the  way  to  a dot  node  binary,  primarily  managed  by the  @napi-rs/cli. This  simplifies  the  build  process considerably.  Lastly,  write  once  parity is  achieved  by  leveraging  shared  node API  specifications,  guaranteeing flawless  cross-compatibility. This  means  modules  developed  with Napi-RS  will  function  identically  across both  Node.js  and  Bun,  eliminating  the need  for  separate  implementations.

---
