---
type: source
videoId: CqBZBJTAo3I
category: development
title: "Building Dynamic Al Interfaces with GenUl"
views: 42
date: 2026-05-31T22:00:11Z
captured: 2026-06-02T12:04:49.287Z
---

# Building Dynamic Al Interfaces with GenUl

> Category: [development](../../videos/development.md) · 42 views · May 31, 2026
> [Watch on YouTube](https://youtu.be/CqBZBJTAo3I)

## Transcript

Welcome  everyone.  I'm  thrilled  to  kick off  today's  session  with  an  exploration into  building  dynamic  AI  interfaces  with GenUI.  Our  focus  will  be  an architectural  deep  dive  into  Hatcha, highlighting  its  role  as  an  open-source showcase  for  Flutter  and  the  Google  ADK. We'll  uncover  how  these  powerful  tools converge  to  create  truly  dynamic  and intelligent  user  experiences.  Hatcha embodies  a  living  experience  actively constructed  around  its  users  to facilitate  social  event  planning. Developed  by  G.  Skinner,  this application  enables  hosts  to  organize events  through  a  comprehensive conversational  interview.  Behind  the scenes,  the  Google  ADK  Python  agent processes  these  conversations, initiating  real-time  generation  of dynamically  themed  invites,  custom components,  and  essential  planning modules.  This  dynamic  process  leads  to the  evolution  of  the  interface,  where  as guests  share  their  preferences,  the  UI composes  new  modules  and  follow-up questions,  continuously  adapting  to match  the  specific  event  context.  The transition  from  conventional  chatbots  to native  generative  UI  represents  a fundamental  reimagining  of  interactive experiences.  This  table  highlights  three key  areas  where  the  GenUI  paradigm, demonstrated  by  Hatcha,  significantly diverges  from  traditional  approaches. Considering  rendering,  traditional chatbots  typically  emit  raw  text, markdown,  or  isolated  web  views.  The GenUI  paradigm,  conversely,  renders strictly  via  native  Flutter  widgets, leveraging  a  unified  GenUI  surface  for cohesive  and  integrated  output.  In  terms of  UI  definition,  traditional  systems rely  on  hard-coded  screens  and  static routes,  which  inherently  limit flexibility.  The  GenUI  approach, however,  allows  AI  to  dynamically  drive the  component  tree,  utilizing  JSON schemas  mapped  to  a  predefined  UI catalog  for  responsive  and  adaptive Regarding  application  state, conventional  chatbots  are  often stateless  or  depend  on  fragile  message history,  hindering  complex  multi-turn interactions.  The  GenUI  paradigm overcomes  this  by  maintaining  a  two-way bound  living  state  or  data  context, which  is  robustly  shared  between  the user  and  the  large  language  model, enabling  truly  persistent  and intelligent  conversations.  This  slide outlines  the  four  core  components  that constitute  the  generative  architecture. First,  the  Flutter  client,  where  the controller  config  resides.  This  element provides  the  per-feature  wiring, effectively  gluing  a  component  catalog to  its  specific  transport  mechanism. Moving  to  the  component  catalogs,  these represent  the  shared  vocabulary  of  our system.  They  maintain  a  strict  registry, dictating  precisely  what  the  AI  is allowed  to  emit  and  what  the  client  is capable  of  rendering. Bridging  these  components  is  the transport  layer,  specifically  utilizing A2UI  over  server-sent  events.  This  is the  connective  tissue  responsible  for streaming  A2UI  JSON  payloads  efficiently across  the  system. And  finally,  we  have  the  Python  agent, which  encapsulates  the  agent  prompt. This  component  serves  as  the  brain, handling  protocol  loading,  managing feature-specific  prompts,  and maintaining  an  A2UI  component  allow list.  The  catalog  enforces  an  absolute boundary  between  AI  and  UI,  a  critical design  principle  where  the  LLM  does  not render  directly.  Instead,  it  declares intent  based  on  a  strict  contract.  This architectural  choice  simplifies development  significantly.  To  introduce a  new  UI  feature,  developers  merely  need to  define  one  schema  and  one  widget, eliminating  the  need  for  any  new application  routing. On  the  server  side,  the  Python  agent leverages  hatcha_catalog.json. This  file  serves  as  a  mirrored vocabulary  of  JSON  JSON precisely  defining  the  components  and properties  that  the  agent  is  permitted to  utilize.  Conversely,  on  the  client side,  our  Flutter  application  defines  a catalog  item.  Each  catalog  item establishes  a  crucial  pairing.  A  JSON schema  representing  the  structured output  emitted  by  the  AI  is  linked directly  with  a  widget  builder,  which specifies  how  that  information  is  drawn natively  within  the  application.  The A2UI  transport  layer  is  specifically designed  for  streaming  generative instructions  with  high  efficiency.  Our protocol,  as  outlined  here,  demonstrates how  an  A2UI  factory  wraps  an  underscore agent  card  ready  connector  to  establish a  persistent  server  sent  event,  or  SSE, connection  directly  to  the  agent.  This continuous  flow,  depicted  from  our server  infrastructure  through  various processing  steps,  including  critical status  updates,  ultimately  reaches  the user's  device. We  ensure  optimized  execution  by  caching connectors  and  agent  card  fetches  based on  URL  and  model.  This  means  each  agent card  is  fetched  exactly  once,  even  in scenarios  involving  concurrent controller  initializations,  thereby conserving  resources  and  improving performance.  Crucially,  for  prompt efficiency,  the  transport  layer  strictly enforces  a  thinking  budget  of  zero.  This mechanism  allows  structured  A2UI  JSON generation  to  execute  instantly, completely  bypassing  the  typically costly  LLM  thinking  tokens. This  design  choice  dramatically  speeds up  response  times  and  reduces computational  overhead.  The  server gatekeeper  is  designed  to  tame  the  often chaotic  outputs  of  large  language models.  The  fundamental  problem  we  face with  LLMs  is  their  inherent unreliability  when  emitting  structured data.  They  frequently  include  extraneous trailing  prose,  hallucinate  incorrect structures,  or  inadvertently  drop necessary  wrappers,  making  their  raw outputs  challenging  to  integrate. Our  solution  centers  around  A2I protocol.py,  which  serves  as  the  single source  of  truth  and  acts  as  a  crucial system  shock  absorber. As  depicted  in  the  funnel  diagram,  A2I protocol.py  takes  in  a  wide  array  of unpredictable  LLM  outputs,  ranging  from raw  text  and  markdown  to  incomplete JSON,  prose,  and  outright During  its  execution,  this  protocol performs  several  critical  functions. First,  it  validates  extension  handshakes using  the  A2I  SDK,  ensuring  secure  and proper  communication. It  also  intelligently  parses  various output  formats,  including  tag-delimited structures,  raw  JSON,  and  code-fenced Most  importantly,  A2I  protocol.py  strips away  any  unnecessary  prose  and  enforces strict  wire  format  normalization, transforming  the  diverse  and  often  messy LLM  outputs  into  a  clean,  predictable, and  usable  format. >> This  slide  our  system  for  auto  repairing broken  payloads  before  they  ever  reach the  client,  ensuring  robust  and compliant  message  processing. Our  critical  rule  dictates  that  every message  must  contain  exactly  one  action key  from  a  predefined  set:  create surface,  update  components,  update  data model,  or  delete  surface. When  a  broken  JSON  payload  enters  the system,  our  protocol  doesn't  reject  it outright.  Instead,  it  intelligently infers  the  correct  wrapper  from  the payload  shape  and  content.  For  example, if  the  payload  contains  both  a  surface ID  and  a  catalog  ID,  the  protocol deduces  it's  intended  for  a  create surface  action  and  wraps  it  accordingly. Similarly,  a  payload  with  surface  ID  and components  will  be  wrapped  in  update components,  and  one  with  surface  ID,  a path,  and  a  value  will  be  interpreted  as an  update  data  model  action. This  inference  process  automatically remediates  the  payload,  transforming  it into  a  fixed  A2II  payload  output  that perfectly  adheres  to  our  protocol, preventing  errors  downstream  and enhancing  system  resilience.  Client composition  fundamentally  leverages independent  GenUI  packages,  a  strategy crucial  for  efficient  development.  The foundation  of  this  approach  is  that  the client  delegates  significant  heavy lifting  to  these  independent  SDK Hatch  specifically  focuses  on  composing these  primitives,  incorporating essential  elements  like  multi-subscriber controllers,  native  styling,  and  robust retry  logic,  rather  than  incurring  the overhead  of  building  the  entire  state machine  from  scratch. The  system  is  structured  in  a  clear, layered  architecture.  At  the  topmost layer,  we  have  the  Hatch  app,  identified as  the  app  GenUI  surface.  This  component acts  as  the  renderer,  recursively traversing  the  UI  tree,  matching  IDs against  a  predefined  catalog,  and  then invoking  widget  builder  to  produce native  Flutter  elements  for  the  user Beneath  this,  the  middle  layer  is represented  by  the  GenUI  SDK.  This  is our  state  engine,  where  the  surface controller  diligently  manages  the  live UI  tree  and  applies  A2UI  actions incrementally,  ensuring  a  responsive  and up-to-date  user  experience.  Crucially, this  layer  also  holds  the  shared  data context,  providing  a  unified  source  of truth  for  the  application  state. Finally,  at  the  base,  we  find  the  GenUI A2A  layer,  which  serves  as  the  essential transport  layer,  handling  all  protocol parsing  necessary  for  communication  and data  exchange.  Two-way  state  binding  is fundamental  to  creating  a  continuous feedback  loop  within  our  system.  The powerful  result  is  a  user  interface  that feels  like  an  instantaneous  guided  form, dynamically  adapting  to  user  inputs  and large  language  model  refinements Central  to  this  architecture  is  the shared  data  context,  which  embodies  our concept  of  shared  truth.  The  SDK maintains  this  shared  data  context  for every  active  surface,  providing  a unified  model  that  both  the  The  and  the AI  agent  can  mutate. Consider  the  user's  interaction.  User writes  occur  when  a  user  engages  with native  widgets,  such  as  a  bubble  select. The  system  then  uses  dispatch  on  change action  to  send  these  selection  changes back  to  the  agent,  updating  the  shared data  context.  Conversely,  the  AI  writes operate  through  the  LLM  patch  mechanism, specifically  via  update  data  model.  The AI  emits  properties  as  references,  and when  the  LLM  patches  a  live  screen,  it intelligently  updates  only  the  specific nodes  affected,  rather  than  rebuilding the  entire  UI  tree.  This  ensures remarkable  efficiency.  This  continuous figure-eight  loop  ensures  that  every user  input  is  immediately  reflected  in the  underlying  model  and  considered  by the  AI,  which  in  turn  provides refinements  that  update  the  UI dynamically,  creating  a  fluid  and  highly responsive  interactive  experience. Native  resilience  mechanisms  are foundational  to  shielding  the  user experience,  and  we  achieve  this  through three  core  pillars.  First,  with  prompt driven  auto  recovery,  the  GenUI controller  automatically  intercepts  any errors.  It  then  appends  the  exact  error text  directly  back  to  the  prompt  and triggers  an  immediate  LLM  retry,  all seamlessly  executed  without  requiring any  user  intervention. For  network  resilience,  a  specialized needs  reconnect  retry  function  actively caches  user  intent.  This  means  that  if connectivity  is  lost,  the  system automatically  resends  the  last  message the  very  moment  the  connection  returns, ensuring  continuity  and  preventing  lost Finally,  ensuring  UI  gracefulness,  GenUI surface  employs  native  shimmer skeletons.  These  overlays  effectively mask  latency  while  the  system  awaits payloads,  which  guarantees  the  interface always  remains  responsive  and  never feels  frozen  or  broken  to  the  user.  This slide  illustrates  the  GenUI  runtime flow,  outlining  a  five-step  process  for how  our  system  processes  user interactions  and  renders  dynamic  UIs. The  flow  begins  with  one  user  triggers. A  UI  screen  calls  send  message  text  and in  response,  the  controller  toggles  the appropriate  loading  states  and  caches the  message  for  subsequent  processing. Next,  we  move  to  two  agent  streams.  The Python  agent  receives  the  user's  prompt and  begins  processing  it.  It  then streams  ATUI  parts  back  to  the  client utilizing  server  sent  events  or  SSE  for efficient  delivery  of  UI  components. This  leads  to  three  controller dispatches.  The  client  extracts  these streamed  parts,  intelligently  auto repairs  any  missing  tags  within  the  UI structure,  and  emits  precisely  typed events  directly  into  the  surface controller,  ensuring  proper communication  and  handling  of  UI Following  this,  four  native  rendering takes  place.  The  GenUI  surface  reads  the active  component  tree,  which  represents the  current  state  of  the  UI.  It  then renders  these  as  native  Flutter  widgets, sourcing  them  directly  from  our predefined  catalog  to  maintain consistency  and  performance. Finally,  the  process  concludes  and continues  with  five  state  syncs.  This crucial  step  involves  two-way  state binding,  actively  synchronizing  any changes  that  occur  between  the  rendered widgets  and  the  agent.  This synchronization  happens  incrementally, efficiently  restarting  the  loop  to ensure  the  user  interface  remains responsive  and  up-to-date  with  the  agent state.  Redefining  scalable  architecture is  achieved  by  rigorously  separating concerns,  leading  to  a  more  efficient and  robust  development  process.  This begins  with  absolute  separation,  where the  large  language  model  strictly declares  intent.  Meanwhile,  Flutter takes  full  responsibility  for  execution and  the  entire  visual  presentation layer.  This  clear  division  ensures  each component  specializes  in  its  core The  benefit  of  this  approach  is frictionless  scalability.  Expanding application  features  no  longer  demands new,  complex  routing  logic.  Instead, developers  can  rapidly  add  capabilities by  simply  crafting  one  prompt,  defining one  JSON  schema,  and  implementing  one Flutter  widget.  This  dramatically streamlines  the  development  workflow. Ultimately,  this  architecture  delivers native  excellence.  The  outcome  is  a highly  contextual  generative  experience that  users  perceive  as  a  meticulously crafted  native  application,  not  a compromised  web  wrapper  or  a  basic  chat window.  This  ensures  a  premium integrated  feel  for  the  end  user.

---
*Captured on Jun 2, 2026.*