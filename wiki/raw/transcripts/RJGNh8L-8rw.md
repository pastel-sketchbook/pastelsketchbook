---
type: source
videoId: RJGNh8L-8rw
category: development
title: "Clean Architecture in Angular"
views: 20
date: 2026-06-23T11:00:37Z
captured: 2026-06-25T17:19:22.671Z
---

# Clean Architecture in Angular

> Category: [development](../../videos/development.md) · 20 views · Jun 23, 2026
> [Watch on YouTube](https://youtu.be/RJGNh8L-8rw)

## Transcript

Welcome  everyone.  Today  we're  diving deep  into  a  crucial  topic  for  building robust  and  scalable  applications,  clean architecture  in  Angular. This  slide  presents  a  modern  blueprint illustrating  how  we  can  implement  clean architecture  principles  within  an Angular  application  using  contemporary tools  like  signals,  repositories,  and interchangeable  adapters. At  its  heart,  clean  architecture  is about  creating  a  system  that  is independent  of  frameworks,  databases, UI,  and  external  agencies,  making  it highly  testable,  maintainable,  and Let's  break  down  this  blueprint.  On  the left,  we  see  the  core  layers.  The presentation  layer  at  the  top  is  where our  user  interface  resides,  handling user  interactions  and  displaying  data. It  communicates  with  the  application core,  which  orchestrates  the application's  use  cases  and  business rules,  acting  as  the  main  bridge  between the  UI  and  the  underlying  domain.  At  the bottom,  the  domain  layer  encapsulates our  core  business  entities  and  rules, remaining  completely  independent  of  any external  concerns.  Moving  to  the  right, we  introduce  how  data  flows  and  is managed.  Signals,  our  reactive  primitive in  Angular,  can  be  effectively  used  for state  management  and  propagating changes.  They  interact  closely  with repositories,  which  define  contracts  for how  our  application  interacts  with  data. These  repositories  are  implemented through  adapters,  which  serve  as  the crucial  interface  between  our application's  core  logic  and  various data  sources,  whether  they  are  APIs, databases,  or  local  storage.  The  beauty of  this  architecture  lies  in  its directional  dependency  rules.  Arrows point  inward,  meaning  inner  layers  have no  knowledge  of  outer  layers.  This modularity  ensures  that  changes  in  the presentation  or  data  storage  mechanisms have  minimal  impact  on  your  core business  logic.  This  blueprint  provides a  solid  foundation  for  developing Angular  applications  that  are  resilient, easy  to  evolve,  and  a  joy  to  maintain. Let's  explore  each  of  these  components in  more  detail.  Examining  the translation  of  functional  state  models, we  can  draw  clear  parallels  between Riverpod  and  Angular  signals.  For instance,  what  Riverpod  refers  to  as  a state  holder,  typically  managed  with  a notifier  provider,  directly  corresponds to  Angular's  signal  function.  When  it comes  to  immutable  updates,  Riverpod patterns  often  involve  creating  a  new state  object  using  state  equals In  Angular  signals,  this  functional update  is  achieved  through  state.update where  a  new  state  is  derived  from  the previous  one  within  the  update  callback. For  derived  state,  Riverpod  utilizes  its provider  mechanism  to  compute  new  values based  on  existing  state.  Angular  signals offer  the  computed  function  for  this exact  purpose,  allowing  you  to  define reactive  values  that  automatically re-evaluate  when  their  dependencies change.  Finally,  managing  side  effects in  Riverpod  often  involves  ref.listen  to react  to  state  changes.  Angular  signals provide  the  effect  primitive  for executing  side  effects  in  response  to signal  value  changes.  It  is  important  to note  that  Angular  signals,  while  using mutable  state  containers  internally, enforce  updates.  A  key  advantage  is their  direct  integration  into  the Angular  framework,  which  inherently eliminates  the  need  for  external providers  or  references  that  might  be common  in  other  state  management solutions.  This  slide  visually demonstrates  how  immutable  updates  are implemented  across  different  modern frameworks  following  a  store  class pattern.  On  the  left,  we  see  Riverpod Dart  code,  where  an  update  name  method immutably  updates  the  state  by  creating a  new  state  object  using  the  copy  with This  approach  ensures  that  the  original state  object  remains  unchanged. Correspondingly,  the  Angular  TypeScript code  on  the  right  achieves  the  exact same  immutability.  Here,  the  update  name method  utilizes  this.user.update with  a  functional  update,  employing  the spread  operator  {dot}{dot}u  to  copy existing  properties  and  then  overwriting specific  ones  like  name. Both  examples  highlight  a  shared architectural  philosophy.  Flutter Riverpod  encourages  the  use  of  immutable state  objects  and  Angular  signals similarly  accomplish  this  pattern through  functional  updates  encapsulated within  a  dedicated  store  class, emphasizing  consistency  and predictability  in  state  management. Comparing  reactivity  implementations  in Dart  and  Angular  showcases  how frameworks  enable  elegant  solutions  for both  derived  state  and  side  effects, significantly  reducing  boilerplate.  For derived  state,  Dart's  Riverpod  allows defining  an  is  adult  provider  by watching  a  user  provider  and  checking the  age  condition.  Similarly,  Angular utilizes  its  computed  function  to declaratively  define  is  adult  based  on providing  a  clear  and  concise  way  to derive  new  state  from  existing  reactive Moving  to  side  effects,  Dart's ref.listen  function  lets  us  easily  react to  changes  in  a  counter  provider,  here simply  printing  the  new  value.  Angular's effect  primitive  serves  a  similar purpose,  executing  a  function  whenever its  dependencies,  such  as  this.counter, change,  enabling  clean  separation  of concerns  for  actions  responding  to  state transitions.  This  diagram  illustrates  an effective  approach  to  structuring application  state  within  a  typical layered  architecture.  At  the  top,  the presentation  layer,  exemplified  by  a to-do  component,  handles  UI  rendering and  user  interactions.  It  consumes  state and  dispatches  actions,  but  does  not manage  the  core  business  logic  or  state Immediately  below  is  the  application layer,  represented  by  a  to-do  store. This  layer  holds  the  application's signal  state  and  the  overarching business  logic  for  the  to-do  feature.  It orchestrates  operations,  translating user  interactions  into  domain  level actions,  and  updating  the  state The  infrastructure  layer,  with components  like  HTTP  to-do  adapter,  acts as  the  bridge  to  external  APIs  and tools.  It's  responsible  for  concrete implementations  of  data  fetching, persistence,  and  other  external Finally,  at  the  core,  we  have  the  domain layer.  This  layer  contains  the  to-do repository  and  to-do  model,  which encapsulate  the  core  business  rules  and entities,  remaining  independent  of  how data  is  stored  or  presented.  The  crucial concept  shown  here  is  dependency inversion,  indicated  by  the  downward arrows.  This  principle  ensures  that higher-level  modules,  such  as  the application  layer,  depend  on abstractions  defined  by  the  domain layer,  rather  than  concrete implementations  in  the  infrastructure layer.  This  inversion  of  control  greatly enhances  the  system's  flexibility, testability,  and  maintainability  by decoupling  the  core  business  logic  from external  dependencies.  Central  to  the domain  layer's  function  is  its establishment  of  the  absolute  contract for  our  application's  core  capabilities. As  demonstrated  by  the  to-do  repository abstract  class  in  to-do.model.ts, this  layer  defines  precisely  what operations  can  be  performed.  Here,  we specify  actions  like  get  all  to  retrieve a  list  of  to-do  items,  add  to  create  a new  one,  toggle  to  change  its  status, and  remove  to  delete  it.  Each  method returns  a  promise,  signifying asynchronous  operations,  and  strongly types  the  expected  outcomes.  This repository  abstraction  is  a  crucial domain  level  contract.  It  strictly dictates  what  the  application  can  do, fundamentally  separating  these  actions from  the  underlying  mechanisms  of  how the  data  is  actually  fetched,  stored,  or manipulated,  ensuring  a  clean  and independent  domain  core.  To  successfully adapt  external  APIs  to  our  domain contract,  we  employ  specific  mechanisms within  the  infrastructure  layer.  The diagram  illustrates  this  by  showing  an external  HTTP  service,  represented  by the  cloud,  feeding  into  the  HTTP  to-do adapter,  which  is  the  gear  icon.  This adapter  then  translates  the  external communication  into  a  format  consumable by  our  core  domain,  symbolized  by  the puzzle  piece  representing  the established  domain  contract.  The  code snippet  provides  a  concrete  example  of this  HTTP  to-do  adapter  in  TypeScript. It's  marked  as  @Injectable,  allowing  for dependency  injection,  and  HTTP  to-do adapter  extends  to-do  repository, indicating  it  fulfills  the  contract defined  by  our  domain.  Inside,  it injects  Angular's  HTTP  client.  The crucial  get  all  method  demonstrates  the transformation.  It  makes  an  HTTP  get request  for  to-do  data.  Since  Angular's HTTP  client  returns  an  RJS  observable, we  use  first  value  from  to  convert  this observable  into  a  standard  promise  of to-do  data,  which  is  the  type  expected by  our  domain's  to-do  repository contract.  Ultimately,  the  infrastructure layer,  where  this  adapter  resides,  is highly  specific  by  design.  It  directly utilizes  tools  like  Angular's  HTTP client  and  RJS's  first  value  from  to manage  all  network  details,  thereby adapting  them  into  the  standardized promises  that  our  domain's  repository expects,  keeping  our  domain  clean  and unaware  of  these  external  intricacies. The  to-do  store  serves  as  a  crucial intermediary,  effectively  connecting reactive  signals  with  our  data repository.  As  shown,  the  to-do  store  is initialized  by  injecting  the  to-do repository,  highlighted  here. This  injection  ensures  that  the  store strictly  adheres  to  an  abstraction layer,  completely  decoupling  it  from  any direct  HTTP  implementation  details,  as noted  at  the  bottom  of  the  slide. Within  the  to-do  store,  we  manage  the list  of  to-dos  using  a  signal, initialized  as  an  empty  array  of  to-do items.  When  a  new  to-do  needs  to  be added,  our  add  method  asynchronously calls  the  add  method  on  the  injected repo  instance.  Once  the  new  to-do  item is  created  by  the  repository,  the this.to-do  signal  is  then  updated.  We use  its  update  function  to  append  the newly  created  to-do  to  the  existing list,  ensuring  our  reactive  state remains  current  and  consistent  without directly  managing  any  network  operations within  the  store  itself.  Wiring  the blueprint  directly  within  the presentation  layer  is  a  crucial  step,  as depicted  on  this  slide.  We  configure  an Angular  component  to  do.component.ts, defining  its  providers.  Observe  how  the to  do  repository  interface  is  bound  to its  HTTP  to  do  adapter  concrete implementation.  This  means  that  at runtime,  the  dependency  injection  system will  supply  an  HTTP  to  do  adapter instance  whenever  to  do  repository  is required.  The  visual  on  the  left effectively  illustrates  this  decoupling. Our  UI  component,  featuring  a  simple button,  communicates  with  a  store through  this  well-defined  interface. Consequently,  the  user  interface interacts  solely  with  the  store's  public methods,  such  as  store.count  and store.add,  maintaining  a  clean separation  from  the  specific  data retrieval  mechanism.  Unlocking plug-and-play  resilience  is  achieved  by clearly  separating  our  core  domain  logic from  its  underlying  implementation details,  much  like  an  electrical  outlet accepting  various  types  of  plugs.  Here, our  to  do  repository,  defined  as  a contract  or  interface,  acts  as  the universal  socket.  It  specifies  what operations  can  be  performed  on  our  to  do items,  but  not  how  those  operations  are carried  out.  This  design  allows  us  to plug  in  different  adapters,  whether  it's an  HTTP  to  do  adapter  for  a  live back-end  API,  an  in-memory  to  do  adapter for  quick  local  development,  or  a  fake async  to  do  adapter  for  robust  testing scenarios.  The  key  benefit  highlighted here  is  that  by  isolating  this  domain contract  from  the  application  state, developers  can  seamlessly  swap  out entire  back-ends.  This  means  switching from  a  testing  environment  to production,  or  even  changing  the underlying  data  persistence  mechanism, can  be  done  without  modifying  a  single line  of  UI  or  core  store  logic.  This drastically  improves  flexibility, testability,  and  the  overall maintainability  of  our  application.  This slide  introduces  the  in-memory  adapter, emphasizing  its  key  characteristics, purity  and  speed.  We  see  an  example implementation  in  TypeScript, defining  an  in-memory  to-do  adapter class  that  extends  a  to-do  repository. It  maintains  a  simple  private  store  as an  array  of  to-do  objects. The  get  all  method,  while  async, highlights  a  crucial  detail.  It  returns structured  clone.this.store. This  design  ensures  that  the  caller receives  a  deep  copy  of  the  data, preventing  any  direct  reference mutations  to  the  adapter's  internal state.  The  add  method,  similarly  async, would  encapsulate  local  memory  logic  for adding  new  items. The  advantages  of  this  approach  are significant.  There  is  no  HTTP  overhead, eliminating  common  asynchronous  timing By  utilizing  a  pure  in-memory  state  and structured  clone,  we  effectively  prevent external  code  from  accidentally modifying  the  adapter's  internal  data. This  meticulously  respects  the repository  abstraction,  guaranteeing that  the  repository's  data  integrity  is maintained  regardless  of  how  it's  used. Achieving  lightning-fast  deterministic testing  is  made  possible  through strategies  like  manual  dependency injection,  as  showcased  in  this example.  Here,  the  before  each  block illustrates  how  an  in-memory  to-do adapter  is  directly  instantiated  and assigned  to  the  repo  property  of  the to-do  store.  This  approach,  labeled manual  DI,  bypasses  the  need  for  complex testing  harnesses.  Consequently,  the  it block,  which  tests  loading  initial to-dos,  can  execute  store.load  and assert  the  expected  to-dos  length  of  two with  complete  isolation  and  speed.  This method,  leveraging  manual  dependency injection  within  Vitest,  ensures  tests run  offline  instantly,  eliminating dependencies  on  Angular  TestBed, external  mocking  libraries,  or  potential network  flakiness.  This  slide demonstrates  how  to  simulate  back-end turbulence  directly  on  your  local machine.  We  achieve  this  by  combining  an in-memory  state  with  an  artificial  delay mechanism,  resulting  in  a  fake  async Looking  at  the  code  snippet,  the  delay function  simply  wraps  set  timeout  within a  promise,  allowing  us  to  introduce arbitrary  network  latency.  Below  that, the  simulate  function  takes  a  value, first  awaits  a  configurable  latency using  our  delay  function,  and  then, crucially,  injects  chaos.  If  this.chaos is  enabled,  there's  a  20%  chance  for  a network  fail  error  to  be  thrown  based  on math.random.  This  setup  allows developers  to  robustly  test  their front-end's  resilience  to  varying network  conditions  and  failures  without needing  a  live  back-end.  The  chaos engineering  dashboard  presents  a  crucial tool  for  front-end  development  and testing.  On  the  left,  we  see  controls for  simulating  real-world  network conditions.  The  latency  slider  is currently  set  to  800  milliseconds,  and chaos  mode  is  activated.  These  settings directly  influence  our  application's behavior,  as  demonstrated  in  the  demo mode  wiring  code  on  the  right.  Here,  the to-do  repository  is  configured  to  use  a fake  async  to-do  adapter,  injecting precisely  that  800-millisecond  delay  and enabling  chaos  with  the  true  flag. This  setup  is  perfect  for  demo environments  and  integration  tests, allowing  developers  to  verify  loading states  and  error  signals  natively without  requiring  a  real  back-end. Choosing  the  right  adapter  is  a  critical decision,  influenced  by  factors  such  as network  dependency,  performance requirements,  and  the  specific  phase  of the  software  life  cycle. We  offer  three  distinct  adapter  types, each  tailored  for  different  scenarios. The  HTTP  adapter,  which  requires  a network  dependency,  is  inherently variable  and  can  be  flaky  in  terms  of speed  and  determinism,  due  to  external network  factors.  This  makes  it  the  ideal choice  for  production  environments  where real-world  network  conditions  are expected.  For  unit  testing,  specifically with  tools  like  Pytest,  the  in-memory adapter  is  superior.  It  has  no  network dependency,  offers  instant  and deterministic  operations,  ensuring  fast and  reliable  test  execution. Finally,  the  fake  async  adapter  is designed  for  local  development  and demos.  It  also  operates  without  a network  dependency,  but  provides configurable  and  deterministic  speed, allowing  developers  to  simulate  various network  conditions  consistently. Ultimately,  this  interchangeability across  adapters  provides  the  correct tool  for  every  phase  of  the  software life  cycle,  ensuring  that  the  UI  and store  remain  entirely  untouched, regardless  of  the  underlying  adapter implementation.  Outlining  a  complete decoupled  ecosystem,  this  slide  details an  Angular  architecture  designed  for unparalleled  modularity  and  testability. The  process  initiates  with  a  component, which  triggers  an  action  like  store.add. This  action  then  flows  to  the  store class,  where  the  application  state  is managed  and  updated  using  functional signal.update  calls.  A  cornerstone  of this  decoupling  is  the  to-do  repository interface.  The  store  class  interacts exclusively  with  this  abstraction, strictly  adhering  to  the  dependency inversion  principle.  This  design  keeps the  core  domain  logic  completely separate  from  external  concerns.  From this  interface,  we  can  easily  swap  in different  concrete  implementations:  an HTTP  adapter  for  live  data,  an  in-memory adapter  for  local  state  management,  or  a fake  async  adapter  for  comprehensive unit  testing.  This  holistic  approach ensures  that  functional  signal  updates seamlessly  power  the  application  state, while  strict  dependency  inversion precisely  isolates  the  domain, culminating  in  a  highly  modular  and infinitely  testable  Angular

---
*Captured on Jun 25, 2026.*