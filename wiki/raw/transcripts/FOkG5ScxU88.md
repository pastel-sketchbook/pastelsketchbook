---
type: source
videoId: FOkG5ScxU88
category: development
title: "Building with Genkit Dart"
views: 51
date: 2026-03-15T19:49:00Z
captured: 2026-04-14T09:43:50.077Z
---

# Building with Genkit Dart

> Category: [development](../../videos/development.md) · 51 views · Mar 15, 2026
> [Watch on YouTube](https://youtu.be/FOkG5ScxU88)

## Transcript

Welcome  everyone.  Today  we  are  going  to dive  into  building  with  Genkit  Dart.  A session  focused  on  empowering  your development  journey  within  the  Flutter and  Dart  ecosystem.  As  AI  continues  to evolve,  having  the  right  framework  to manage  complexity  is  essential. Throughout  this  presentation,  we  will explore  three  foundational  concepts. Model  agnostic  AI,  which  offers  the flexibility  to  switch  between  different large  language  models.  type  safe  flows which  ensure  robustness  and  catch  errors early  in  the  development  cycle  and  tool calling  which  enables  your  AI  to interact  intelligently  with  external systems.  I  look  forward  to  showing  you how  these  features  can  transform  your approach  to  building  modern  intelligent applications.  Documenting  a  full  stack AI  architecture  requires  understanding two  distinct  interaction  patterns. First,  the  remote  model  chat  serves  as  a free  form  conversation  interface.  It illustrates  the  workflow  of  securely proxying  raw  prompts  from  a  Flutter front  end  to  the  Gemini  model  via  a robust  Dart  backend  emphasizing  secure communication  patterns.  In  contrast,  the remote  action  flow  functions  as  a structured  AI  planner.  This implementation  demonstrates  the  use  of type-  safe  inputs  and  sophisticated serverside  tool  calling  such  as integrated  weather  and  image  search capabilities.  Furthermore,  it  highlights the  generation  of  formatted  markdown content  within  a  structured  execution flow.  Together,  these  components  provide a  comprehensive  framework  for  modern  AI application  development.  The architecture  presented  here  illustrates a  unified  Dart  stack  spanning  from  the client  to  the  model.  This  approach creates  a  seamless  development environment  by  utilizing  Dart  across every  layer  of  the  application.  At  the front  end,  Flutter  delivers  a  consistent cross-platform  user  interface  while integrating  the  Genkit  client  SDK. Moving  to  the  server  side,  the  Dart backend  leverages  Genkit,  shelf,  and schematic  to  manage  routing,  types  safe schemas,  and  sophisticated  AI  flows. This  entire  system  is  powered  by  Google Genai,  Gemini  models  at  the  provider level.  By  utilizing  Melos  RS  for monorreo  management,  developers  can efficiently  bridge  the  client  and backend  components.  The  primary  benefit of  this  unified  stack  is  the  ability  to share  data  schemas  across  the  entire ecosystem,  ensuring  end-to-end  type safety  and  completely  removing  the overhead  of  manual  JSON  serialization. When  selecting  the  right  integration architecture,  we  evaluated  several options  to  determine  the  best  path forward.  First,  we  looked  at  using  the direct  Gemini  REST  API.  However,  this approach  lacks  type  safety  and  flow abstraction,  necessitating  tedious manual  HTTP  client  code.  Next,  we considered  an  all-in  Flutter architecture  with  no  serverside component.  We  rejected  this  immediately as  it  requires  embedding  API  keys directly  in  the  client,  which  is  a massive  security  risk.  We  also  explored heavy  server  frameworks  such  as  server pod,  but  concluded  they  were  overkill for  simple  AI  proxying.  Our  chosen  stack is  genkit  combined  with  shelf.  This provides  the  benefit  of  a  unified  dart stack  and  type-  safe  interactions  while maintaining  secure  serverside  key management.  To  understand  how  genkit operates,  we  should  look  at  its  four core  abstractions.  Models,  flows,  tools, and  schemas.  Models  provide  model agnostic  APIs  enabling  developers  to switch  between  providers  like  Google Gemini  and  Anthropic  Cloud  with  a  single line  of  code.  Flows  are  type-  safe, observable,  and  deployable  functions that  wrap  AI  logic  and  complex  business rules  into  a  unified  structure.  Tools refer  to  back-end  functions  such  as fetching  weather  or  searching  for  images that  an  LLM  can  call  midgeneration  to enhance  its  output.  Lastly,  schemas ensure  consistency  by  providing  strongly typed  input  and  output  shapes  shared across  the  client  and  server, effectively  preventing  data  mismatches. There  are  two  primary  ways  to  integrate AI  into  your  applications,  each  suited for  different  functional  requirements. First  is  the  remote  model  proxy,  which is  the  standard  approach  for  chatbased interfaces.  In  this  scenario,  the  client application  makes  a  call  to  a  generation function  and  the  server  simply  acts  as  a conduit  forwarding  the  raw  prompt directly  to  the  underlying  model  such  as Gemini.  This  lightweight  integration  is best  for  open-ended  conversations  where a  direct  response  from  the  model  is required.  The  second  pattern  is  the remote  action  which  is  more  appropriate for  complex  use  cases  like  a  trip planner.  Instead  of  a  direct  prompt,  the client  triggers  a  specific  action  on  the server.  The  backend  then  orchestrates  a complex  flow,  leveraging  tools  and schemas  to  process  the  request.  This results  in  a  response  of  strongly  typed markdown.  This  pattern  is  the  optimal choice  for  structured  multi-step  tasks that  demand  greater  control  and  data consistency.  Schematic  provides  a  robust mechanism  for  guaranteeing  type  safety across  your  entire  application  stack. The  process  begins  with  defining  an abstract  class  such  as  trip  request which  serves  as  the  single  source  of truth  for  your  data  models.  By  utilizing the  at  schema  annotation  and  running  the buildrunner  tool,  we  automatically generate  a  concrete  class  in  this  case trip  request.sema which  includes  built-in  validation logic.  This  generated  schema  is  then shared  between  the  Flutter  front  end  and the  backend,  such  as  a  genkit  shelf implementation. Using  the  dollar  prefix  convention prevents  naming  collisions  between  your source  definitions  and  the  generated code,  ensuring  that  the  client  and server  remain  perfectly  synchronized  and type-  safe.  Evolving  AI  with mid-generation  tool  calling  represents  a paradigm  shift  in  how  large  language models  interact  with  external environments.  To  enable  this  capability, tool  descriptions  are  injected  directly into  the  system  prompt,  which  instructs the  model  on  exactly  when  and  how  to invoke  specific  functions.  The  process follows  a  five-step  cycle.  First,  the  AI receives  a  user  prompt.  When  the  model determines  that  it  requires  external data  to  provide  an  accurate  response,  it halts  generation  at  step  two  to  request a  specific  tool,  such  as  a  function  to fetch  current  weather  data.  In  step three,  the  server  executes  the  requested dart  function.  Once  the  task  is completed,  step  four  involves  the  server returning  the  tools  output  back  to  the AI.  Finally,  in  step  five,  the  AI resumes  its  generation,  incorporating this  newly  injected  real  world  data  to deliver  a  more  precise  and  informed final  output.  Let's  examine  the  critical API  distinctions  between  model  and  model ref.  These  types  serve  different purposes  and  cannot  be  used interchangeably.  On  one  hand,  we  have model  ref  of  t.  This  type  is  obtained through  the  Google  AI.Ini  function.  Its primary  use  case  is  within  internal flows  specifically  for  AI.generate. You  should  think  of  a  model  ref  as  a lightweight  reference  that  is  resolved at  generation  time.  On  the  other  hand, we  have  the  model  action.  This  is obtained  via  the  Gemini  plugin.mmodel call.  It  is  specifically  required  when you  need  to  expose  a  model  as  an  HTTP endpoint  using  shelf  handler  model. Unlike  the  lightweight  reference,  the model  action  is  a  heavyweight  action subtype  necessary  for  proper  routing.  It is  important  to  emphasize  that  these types  are  not  interchangeable.  If  you attempt  to  pass  a  model  ref  into  a  shelf handler,  it  will  result  in  a  fatal  type error.  Always  ensure  you  are  utilizing the  appropriate  type  for  your  specific implementation  context.  The  canvas  getit cross  origin  image  constraint  is  a critical  issue  that  occurs  when  building flutter  web  applications  using  the canvas  git  renderer  as  illustrated  here. When  attempting  to  pull  images  from  an external  source  like  the  wikimedia  API, the  system  encounters  a  silent  failure. Even  if  the  external  server  is  correctly configured  with  corors  headers,  the image.net  network  widget  fails  to  render images  from  non-localhost  URLs.  The  most challenging  aspect  of  this  constraint  is its  symptom.  There  are  no  console  errors or  exceptions  triggered.  Images  simply disappear  from  the  UI,  leaving developers  without  standard  debugging logs  to  identify  the  root  cause  of  the missing  assets.  The  challenge  of  cross origin  resource  sharing  or  cores  often arises  when  a  large  language  model generates  markdown  content  featuring external  links  such  as  those  from Wikipedia.  To  solve  this  within  a Flutter  environment,  we  implement  a serverside  URL  rewriting  strategy. First,  the  LLM  generates  the  markdown containing  these  external  Wikipedia URLs.  Before  this  content  reaches  the client,  our  server  utilizes  regular expressions  to  intercept  the  image syntax.  We  process  these  matches  in reverse  order  to  ensure  that  string offsets  remain  accurate  as  the  text  is modified.  Next,  the  server  proactively fetches  the  raw  image  bytes  from  the external  source  and  stores  them  within an  in-memory  cache.  This  localized storage  ensures  high-speed  delivery  and minimizes  repeated  external  requests. Finally,  the  original  external  URL  is rewritten  to  a  localized  endpoint  such as  http/lohost/appi/im images  followed  by  a  unique  identifier. By  delivering  these  rewritten  URLs  to the  Flutter  application,  we  effectively bypass  cores  restrictions  as  the  client is  now  requesting  assets  directly  from  a trusted  internal  source.  To  create  a dynamic  and  visually  interesting interface,  we  use  programmatic generation  for  our  scrapbook  UI elements.  For  the  tape  pieces,  we utilize  the  hash  code  of  the  URI  as  a random  seed.  This  allows  us  to consistently  select  from  five  distinct pastel  colors  at  77%  alpha  transparency and  apply  a  unique  rotation  angle ranging  from  minus0.21 to  plus  0.24  radiance  to  give  the elements  physical  weight.  We  have implemented  a  twolayer  depth  system using  box  shadows.  This  includes  a  12 pixel  blur  for  perceived  depth  and  a more  subtle  four  pixel  blur  to  provide physical  grounding  against  the background.  Finally,  to  ensure  a  smooth user  experience,  we  employ  graceful loading  techniques  by  setting  a  minimum height  constraint  of  120  pixels.  We prevent  the  layout  from  jumping  or shifting  as  the  server  caches  cross origin  images,  maintaining  a  stable visual  flow.  Our  focus  on  a  streamlined workspace  and  key  management  is  designed to  reduce  friction  for  developers. First,  we  have  implemented  zero  config authentication.  The  Genkit  Google  Genai plugin  automatically  detects  both  Gemini API  key  and  Google  API  key  upon initialization,  ensuring  you  are  ready to  build  immediately.  Furthermore,  our workspace  commands  help  isolate dependencies  while  enabling  global execution.  By  using  Melo's  RSBS,  you  can bootstrap  the  environment.  And  with Melo's  RS  rundev,  you  can  launch  the server  and  Flutter  apps  simultaneously, allowing  for  a  seamless  and  integrated development  experience.  As  we  analyze the  key  findings  from  our  development process,  four  critical  technical  lessons emerge.  First,  it  is  important  to  note that  max  turns  defaults  to  five.  If  your flow  relies  on  multiple  sequential  tool calls,  such  as  fetching  weather  data followed  by  generating  several  images, you  should  increase  this  limit  to  15  or more  to  prevent  silent  truncation  of  the process.  Second,  regarding  library structure,  be  aware  of  specific  import paths.  The  define  remote  action  function resides  within  package genkit/client.dart rather  than  the  main  barrel  file.  Using the  correct  path  is  essential  for  proper function  resolution.  Third,  to  ensure optimal  resource  management  and  avoid connection  leaks,  you  must  explicitly call  remoteaction.  Disposal  within  your widgets  life  cycle.  This  step  is mandatory  to  properly  clean  up  the underlying  HTTP  client.  Finally,  we  have observed  that  prompt  engineering  is  not a  foolproof  solution  for  preventing  tool bypassing  because  LLMs  may  still  embed external  URLs  from  their  training  data. Implementing  serverside  reax post-processing  is  a  necessary  security measure  to  ensure  data  integrity. Looking  at  the  road  map  for  the  genkit architecture,  we  are  focusing  on  four key  pillars  of  enhancement.  First,  we are  enabling  streaming  responses  using the  generate  stream  function  to  provide real-time  token  delivery  for  a  more responsive  user  experience.  Second,  we are  prioritizing  structured  output through  schemas,  ensuring  that  models return  typed,  predictable  JSON  data  for complex  tasks  like  day-by-day  planning. Third,  we  are  introducing  multi-turn memory  to  maintain  conversational context  even  when  coordinating  across multiple  remote  actions.  Finally,  we  are expanding  our  provider  ecosystem  with dedicated  plugins  for  anthropic  and  open AAI.  Underpinning  all  these  updates  is  a core  design  philosophy.  The  architecture remains  modular,  allowing  you  to  swap models  and  capabilities  with  a  single line  of  code,  effectively  preventing vendor  lock

---
*Captured on Apr 14, 2026.*