---
type: source
videoId: mGMaqTvWrCc
category: kubernetes
title: "Building an End-to-End MLOps Pipeline"
views: 20
date: 2026-04-14T23:21:34Z
captured: 2026-04-17T03:54:00.713Z
---

# Building an End-to-End MLOps Pipeline

> Category: [kubernetes](../../videos/kubernetes.md) · 20 views · Apr 14, 2026
> [Watch on YouTube](https://youtu.be/mGMaqTvWrCc)

## Transcript

Welcome  everyone.  Thank  you  for  joining us  today.  In  this  session,  we  will  be diving  into  the  essential  process  of building  an  end-to-end  MLOps  pipeline. We  will  trace  the  entire  life  cycle  of  a machine  learning  model  starting  from  the training  phase  and  progressing  through to  deployment  on  managed  endpoints within  Microsoft  Azure.  Our  goal  is  to provide  you  with  a  clear  roadmap  for operationalizing  your  machine  learning workflows  effectively  and  at  scale. Let's  get  started.  The  MLOps  imperative is  defined  by  the  critical  shift  from manual  experimentation  to  automated production.  On  the  left,  we  examine  the notebook  trap.  While  Jupyter  notebooks are  excellent  for  initial  exploration, relying  on  them  for  ad  hoc  model training  creates  a  chain  of  operational problems.  Without  versioning  or reproducibility  and  hampered  by  hidden dependencies,  there  is  no  automated  path to  production.  This  creates  a  high-risk environment  where  if  the  author  is  away, the  pipeline  simply  stops.  In  contrast, the  pipeline  solution  on  the  right represents  a  well-designed  MLOps architecture.  This  approach  ensures  that training  is  repeatable.  All  artifacts are  versions  and  deployment  is  fully automated.  By  implementing  these practices,  the  path  from  a  code  change to  a  live  endpoint  is  streamlined  into  a single  reliable  merge  to  main,  providing the  necessary  foundation  for  scalable and  sustainable  machine  learning.  To determine  if  this  pipeline  architecture is  the  right  fit  for  your  project,  let's review  the  applicability  checklist.  This pattern  is  ideal  when  your  training  data is  stored  in  Azure  blob  storage, specifically  in  parquet  or  CSV  formats. It  is  designed  for  standard  Python machine  learning  frameworks  like scikitlearn.  You  should  choose  this approach  if  you  need  versioned  model artifacts  managed  in  a  central  cross team  registry  or  if  you  require  an automated  deployment  path  to  a  live scoring  endpoint.  Furthermore,  it  is  the preferred  choice  when  downstream consumers  require  a  reliable  handoff mechanism  for  consistent  operations. On  the  other  hand,  there  are  specific cases  where  you  should  consider  a different  pattern  for  distributed training  workflows.  Azure  machine learning  pipelines  are  the  more appropriate  tool.  Additionally,  while models  requiring  specialized  GPU inference  configurations  are  supported, they  necessitate  a  different configuration  structure  than  the  one outlined  in  this  specific  pattern,  use these  criteria  to  ensure  you  are selecting  the  most  efficient architecture  for  your  machine  learning life  cycle.  This  slide  illustrates  our four-  stage  architecture,  which automates  the  translation  of  raw  code into  a  managed  prediction  API.  This entire  life  cycle  executes  sequentially upon  every  push  to  the  main  branch, ensuring  a  continuous  and  reliable deployment  pipeline.  The  process  begins with  a  DevOps  gate.  This  initial  stage serves  as  a  validation  layer,  verifying pipeline  execution  and  logging  built numbers  to  maintain  rigorous  version control  and  auditability.  In  the  second stage,  train  and  publish,  the  system automatically  installs  the  required dependencies  and  executes  training scripts  against  data  stored  in  blob storage.  Upon  successful  completion,  the resulting  model  is  published  as  a  pickle bundle.  Next,  we  move  to  register  an  ML registry.  This  stage  involves downloading  the  model  artifact  and registering  it  within  our  centralized model  registry,  utilizing  auto versioning  to  track  every  iteration  of the  model's  development.  Finally,  the pipeline  concludes  with  deploy  to endpoint.  Here,  the  system  provisions  or updates  a  managed  online  endpoint, facilitating  real-time  scoring  and ensuring  the  latest  model  is  immediately available  for  production  use.  The streamlined  workflow  allows  for  rapid automated  deployment  while  maintaining high  standards  of  operational excellence.  Stages  one  and  two  focus  on the  design  and  execution  of  the  training script.  Our  core  principle  is  that  the script  must  be  a  standalone  Python  CLI. By  ensuring  it  reads  credentials  from environment  variables,  we  guarantee  it remains  runnable  locally  before  it  ever touches  the  production  pipeline.  The operational  cycle  begins  with  loading data  from  Azure  blob  storage  using  ADLFS and  PI  arrow.  Once  loaded,  we  validate the  schema  to  verify  columns,  data types,  and  row  counts.  This  ensures  we fail  fast  if  the  input  data  is incorrect.  We  then  move  to  feature engineering  where  we  compute  derived columns,  handle  missing  values,  and encode  categories.  With  our  features prepared,  we  proceed  to  train  the  model using  a  chosen  framework  such  as scikitlearn. Following  model  training,  we  apply pre-processing  to  fit  scalers  like standard  scaler,  ensuring  they  are captured  for  scoring  reuse.  Finally,  we serialize  the  output  by  bundling  the model  and  metadata  into  a  pipeline artifact  via  a  designated  output  path. This  modular  approach  ensures consistency  and  portability  throughout the  machine  learning  life  cycle.  When  we talk  about  unpacking  the  model  bundle, we  are  essentially  defining  a  scoring contract.  This  self-contained  package ensures  that  downstream  consumers  can score  new  data  effectively  without needing  deep  knowledge  of  the  training process  which  helps  eliminate  silent bugs  in  production  environments.  Inside this  bundle,  we  typically  include  four critical  components.  First  is  the  model itself  such  as  a  trained  scikitlearn random  forest  classifier  containing  the learned  logic.  Second  is  the  scaler,  the exact  pre-processor  fitted  during training  to  ensure  incoming  data  is normalized  or  transformed  correctly before  it  reaches  the  estimator.  Third, we  include  the  feature  order,  which  is  a list  of  strings  that  dictates  the  exact column  sequence.  This  prevents  silent errors  caused  by  data  being  reordered unexpectedly  in  the  production  pipeline. Finally,  we  include  comprehensive metadata  for  operational  health.  This includes  a  timestamp  of  when  the  model was  trained  to  help  debug  stale predictions,  source  and  clean  row  counts to  track  data  attrition  and  detect pipeline  failures,  and  the  specific scikitlearn  version  to  prevent compatibility  issues  when  unpickling  the model.  Together,  these  elements  create  a robust,  predictable  bridge  between  model training  and  real  world  scoring.  When choosing  a  serialization  format  for  our machine  learning  models,  we  must  weigh the  specific  requirements  of  our deployment  environment.  This  diagnostic matrix  compares  three  primary  options. Pickle,  joblib,  and  onyx.  Pickle  is  the format  used  in  our  current  pipeline.  It is  ideal  for  creating  comprehensive bundles  that  include  the  model  itself along  with  essential  metadata  such  as scalers,  feature  ordering,  and configuration  files.  Since  it  is  built directly  into  Python,  it  requires  zero extra  dependencies,  simplifying  our environment  management.  If  we  encounter models  that  are  heavily  reliant  on  large numpy  arrays,  job  liib  becomes  a  strong alternative.  It  provides  significantly faster  performance  for  large  arrayheavy data  structures.  Though  it  does introduce  an  external  dependency  to  the project  for  scenarios  requiring  cross framework  interoperability  such  as moving  a  model  between  PyTorch  and scikitlearn.  Onyx  is  the  preferred standard.  While  it  offers  high portability  across  different  platforms, it  is  important  to  note  that  it  lacks support  for  certain  specialized  model types.  Finally,  we  must  address  the critical  security  warning  at  the  bottom of  the  matrix.  Never  load  pickle  files from  untrusted  sources  as  the deserialization  process  can  be  exploited to  execute  arbitrary  code.  To  maintain  a secure  production  environment,  always validate  the  providence  of  your  files via  an  access  controlled  ML  registry. Let's  examine  the  foundational architectural  decisions  for  our  pipeline YAML  structure  focusing  on  security, efficiency,  and  scalability.  First, regarding  credential  injection,  our objective  is  to  move  beyond  storing sensitive  credentials  in  DevOps  variable groups.  While  injecting  them  through environment  blocks  is  a  common  practice, the  superior  approach  is  to  utilize  a user  managed  identity  paired  with  the default  Azure  credential  class.  This transition  allows  us  to  eliminate secrets  entirely  from  our  configuration, significantly  improving  our  overall security  posture.  Moving  to  the  center, we  have  decoupled  stages.  By  strictly separating  the  training  and  registration phases,  we  gain  valuable  operational flexibility.  The  training  artifact  is published  as  a  distinct  output  between stages.  This  means  that  if  a registration  step  fails,  we  can  resolve the  issue  and  rerun  only  the registration  process  without  wasting time  and  compute  resources  on retraining.  Additionally,  this architecture  ensures  that  the  specific training  artifact  is  always  available for  local  debugging  and  validation. Finally,  we  are  centralizing  model management  through  Azure  ML  registries and  auto  versioning.  By  utilizing  the Azure  CLI  AML  model  create  command  with the  registry  name  parameter,  we  can promote  models  to  a  registry  that  is shared  across  various  workspaces.  This facilitates  better  collaboration  across teams.  Furthermore,  since  pushing  a model  with  an  existing  name automatically  increments  its  version,  we achieve  consistent  and  automated  life cycle  management  without  manual intervention.  Stages  three  and  four focus  on  deploying  your  model  to  a managed  online  endpoint.  A  significant advantage  of  this  approach  is  that  Azure machine  learning  handles  the  entire underlying  infrastructure  including provisioning  compute,  load  balancing, autoscaling,  and  performing  health probes.  The  workflow  begins  when  a client  sends  an  HTTP  request  in  JSON format  to  a  static  HTTPS  URL.  This request  is  received  by  the  managed endpoint  which  acts  as  the  gateway  for authentication  and  load  balancing.  The endpoint  provides  a  stable  DNS  name, ensuring  that  client  calls  remain consistent  even  as  the  backend infrastructure  evolves.  From  the endpoint,  traffic  is  routed  to  the deployment  compute.  This  represents  the actual  provisioned  resources  running your  scoring  code,  which  typically consists  of  a  score.  pycript  and  the model.pkl  file.  This  architecture  is highly  flexible  supporting  multiple deployments  such  as  blue  green strategies  under  a  single  endpoint  to facilitate  safe  and  seamless  updates. Finally,  once  the  model  processes  the input,  the  system  generates  a  structured JSON  response  to  be  sent  back  to  the client.  The  score.py  script  serves  as the  essential  architectural  glue, bridging  the  gap  between  the  managed endpoint  and  the  encapsulated  model bundle.  It  operates  according  to  a strict  contract  defined  by  two  core functions  init  and  run.  The initialization  phase  handled  by  the  init function  is  triggered  just  once  when  the container  starts  up.  A  major  advantage of  this  framework  is  how  Azure  machine learning  automatically  retrieves  the required  artifacts  from  the  registry  and places  them  in  the  local  Azure  ML  model deer.  This  abstraction  ensures  that developers  do  not  need  to  manage  storage URLs  within  their  scoring  code. significantly  simplifying  the  deployment process.  Once  the  environment  is initialized,  the  run  function  handles each  incoming  request.  Within  this function,  we  enforce  critical consistency  checks  to  maintain  model integrity.  First,  we  apply  feature enforcement  by  referencing  the  original feature  order  stored  in  our  model bundle,  ensuring  that  incoming  JSON  data is  mapped  precisely  to  the  training schema.  Second,  we  perform  stateful pre-processing  by  reusing  the  bundled standard  scaler.  This  ensures  that  every numerical  transformation  perfectly matches  the  logic  applied  during  the training  phase  which  is  fundamental  to preventing  training  serving  skew  and maintaining  model  accuracy  in production.  Our  security  profile  for this  architecture  is  built  on  the principle  of  least  privilege. Authentication  is  handled  through  a  user managed  identity  or  UMI  which  is  linked via  workload  identity  federation.  This approach  is  critical  as  it  eliminates the  need  for  stored  secrets  and  ensures that  zero  contributor  or  owner  level access  is  required  for  standard operations.  As  detailed  in  this permissions  matrix,  we  have  established three  distinct  roles  to  govern  access. First,  the  storage  blob  data  reader  role is  applied  at  the  storage  account  or container  level,  granting  the  specific permissions  necessary  to  read  raw training  data.  Next,  the  Azure  ML registry  user  role  is  scoped specifically  to  the  ML  registry.  This allows  for  the  pushing  and  versioning  of model  artifacts  while  maintaining  strict boundaries  around  other  resources. Finally,  the  Azure  ML  data  scientist role  is  utilized  within  the  ML  workspace scope.  This  role  is  purposefully  defined to  enable  the  creation  and  updating  of managed  endpoints  and  their  underlying deployments.  By  implementing  these granular  permissions,  we  maintain  a robust  and  compliant  security  posture throughout  the  entire  machine  learning life  cycle.  When  navigating  common pitfalls  in  diagnostics,  we  first address  column  mismatches.  Data  in  paret files  may  use  period  ID  while  scripts expect  period  ID.  It  is  essential  to implement  case  and  sensitive  renaming and  strict  schema  validation  before training  begins.  Next,  be  aware  of Windows  agent  limitations.  Self-hosted Windows  agents  utilize  cmd.exe, meaning  bash  style  line  continuations will  fail.  Always  use  single  line commands  or  explicit  PowerShell  syntax to  ensure  compatibility.  Regarding repository  checkout,  always  use  checkout self  when  the  pipeline  configuration  is stored  in  the  training  repository.  Named checkouts  pull  from  the  default  branch which  can  lead  to  feature  branch pipelines  running  stale  code.  Finally, to  mitigate  dependency  drift,  pin  all dependencies  in  your  requirements.txt. Even  a  minor  version  update  in  libraries like  scikitlearn  can  silently  alter model  behavior  or  break  pickle deserialization.  As  organizations  evolve their  machine  learning  capabilities, they  move  beyond  basic  workflows  toward an  advanced  MLOps  maturity  path.  At  the center  of  this  progression  is  the  core four-stage  pipeline  which  serves  as  the foundation  for  several  critical operational  extensions.  To  ensure quality  control,  we  implement  model validation  gates.  These  gates automatically  evaluate  model  performance against  holdout  data  sets,  blocking  any deployment  that  fails  to  meet  predefined minimum  thresholds.  Moving  to  the deployment  phase,  multi-environment promotion  provides  a  structured  path  for model  delivery.  This  involves registering  models  in  development, deploying  them  to  staging  for validation,  and  moving  through  testing before  final  promotion  to  the  production environment.  To  evaluate  models  in  real world  scenarios,  we  utilize  AB  testing. By  splitting  live  deployment  traffic,  we can  directly  compare  new  models  against existing  ones  to  measure  their  actual impact  on  business  metrics.  For  high volume  requirements,  we  leverage  batch scoring.  This  allows  us  to  use  dedicated batch  endpoints  to  process  and  score massive  data  sets  on  a  scheduled  basis, optimizing  resource  usage  for non-realtime  tasks.  Finally,  we  ensure long-term  reliability  through  drift monitoring.  By  tracking  prediction distributions  over  time,  the  system  can identify  performance  degradation  and automatically  trigger  retraining  cycles. Together,  these  integrated  components transform  a  standard  pipeline  into  a resilient  and  scalable  MLOps  ecosystem. Establishing  a  repeatable  path  to production  is  essential  for  scalable machine  learning.  This  workflow  follows four  key  stages.  Train,  register, deploy,  and  score.  This  standardized structure  remains  identical  regardless of  the  model's  complexity,  whether  we are  deploying  a  simple  classifier  or  a sophisticated  anomaly  detector.  By leveraging  automation,  a  single  push  to the  main  branch  triggers  a  versioned  and secure  journey.  This  process  takes  us from  raw  data  to  a  live  productionready HTTPS  production  endpoint,  ensuring reliability  and  consistency  across  all machine  learning  initiatives.

---
*Captured on Apr 16, 2026.*