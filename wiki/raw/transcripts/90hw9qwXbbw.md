---
type: source
videoId: 90hw9qwXbbw
category: development
title: "Architecting with Tonic"
views: 19
date: 2026-05-02T09:48:38Z
captured: 2026-05-03T02:03:51.660Z
---

# Architecting with Tonic

> Category: [development](../../videos/development.md) · 19 views · May 2, 2026
> [Watch on YouTube](https://youtu.be/90hw9qwXbbw)

## Transcript

Welcome.  It  is  a  pleasure  to  have  you here  for  today's  session  where  we  will explore  architecting  with  Tonic,  a structural  blueprint  of  Rust's high-performance  gRPC  framework.  In  this presentation,  we  will  dive  deep  into  the architecture  of  Tonic,  examining  how  it harnesses  the  power  of  the  Rust programming  language  to  provide  a  fast, safe,  and  efficient  environment  for building  gRPC  services.  We  will  look  at its  core  components  and  discuss  the structural  design  patterns  that  make  it a  premier  choice  for  high  performance systems.  Let's  get  started.  Tonic achieves  high  flexibility  by  abstracting asynchronous  operations  through  a dedicated  runtime  trait.  This architecture  ensures  that  the  core  gRPC logic  encompassing  both  client  channels and  server  implementations  remains decoupled  from  the  specific  execution details  of  the  underlying  system.  By utilizing  a  runtime  trait  adapter,  Tonic provides  a  standardized  interface  for four  essential  categories  of asynchronous  tasks.  Task  spawning, network  IO,  time  management,  and  DNS resolution.  While  this  abstraction  layer allows  for  future  extensibility,  the current  implementation  relies  heavily  on the  Tokyo  ecosystem  for  concrete execution.  At  the  bottom  of  the  stack, you  can  see  the  specific  components  that fulfill  these  traits,  such  as  Tokyo runtime  for  task  execution,  Tokyo  TCP stream  for  network  communication,  and specialized  resolvers  like  the  Tokyo default  DNS  resolver,  which  integrates with  Hickory  resolver  for  robust  DNS lookups.  This  modular  approach  allows Tonic  to  maintain  a  clean  core  while leveraging  the  high  performance capabilities  of  the  Tokyo  runtime.  We will  now  examine  the  protocol  and  core abstractions.  These  unified  traits  are designed  to  manage  message  framing, metadata  encapsulation,  and  response streaming.  Crucially,  they  operate independently  of  the  underlying serialization  payload,  ensuring flexibility  across  different  data formats.  On  the  left,  we  see  the workflow  for  message  handlers.  A  message enters  the  pipeline  through  the  send message  trait.  Note  the  interim  step  for downcasting  which  allows  for  inspection of  the  message  content.  On  the  receiving side,  the  Rev  message  trait  handles incoming  data  utilizing  mutable downcasting  to  convert  the  generic payload  back  into  a  specific  message type.  Moving  to  the  right,  we  outline the  call  life  cycle.  It  begins  with  a request  which  includes  a  metadata  map for  headers.  The  response  is  handled  as a  response  stream  item,  a  structured container  that  sequences  the  flow  from initial  headers  through  a  continuous stream  of  messages  and  finally  to  the trailers.  The  life  cycle  concludes  with a  status  object  which  encapsulates  the final  status  code  and  any  associated binary  details.  This  architecture provides  a  robust  framework  that decouples  transport  logic  from application  specific  data  structures. The  Protobuff  compilation  pipeline provides  the  essential  transformation  of Proto  schemas  into  executable  client  and server  steps.  This  process  utilizes  a highly  configurable  chain  of  product plugins  and  tonic  build  crates  to  ensure seamless  integration.  It  begins  with  the source.protoiles  which  are  first processed  by  the  proto  compiler.  During this  phase,  the  compiler  exports  a  file descriptor  set  which  serves  as  a comprehensive  binary  representation  of the  schema.  Subsequently,  the  Prodocch Rust  gRPC  C++  plug-in  analyzes  the specific  service  methods  defined  within that  schema.  The  pipeline  then  moves into  the  Rust  specific  phase  with  tonic prost  build  where  specialized configurations  are  applied  to  tailor  the output.  The  ultimate  result  is  a  set  of generated  code  comprising  idiomatic  rust strus  for  data  representation  prost codec  implementations  for  efficient serialization  and  the  service  traits necessary  for  implementing  our  gRPC logic.  Tonic  provides  multiple integration  paths  for  code  generation depending  on  whether  you  require  prost integration  custom  protobuff  interfaces or  manual  service  definitions.  The  first option  tonic  prost  build  focuses  on seamless  prost  integration.  It  includes advanced  features  such  as  external  type mapping  via  extern  path,  the  skip  debug option  for  cleaner  generated  code  and use  arc  self  for  implementing  services with  arc  wrapped  receivers.  Moving  on, gRPC  protobuff  build  specifically targets  the  protobuff  crate.  This configuration  is  responsible  for orchestrating  C++  plug-in  compilation and  allows  for  a  modular  approach  by separating  the  generation  of  messages and  services.  Lastly,  for  projects  that need  to  bypass  protoiles  altogether,  we have  the  manual  tonic  build  path.  This utilizes  service  builder  and  method builder  to  enable  custom  codec  path injection,  providing  developers  with maximum  flexibility  for  specialized service  implementations.  Tonic integrates  seamlessly  with  tower middleware  enabling  the  non-destructive injection  of  crosscutting  concerns  such as  authentication  and  telemetry.  Looking at  the  request  flow,  we  begin  on  the client  side  where  a  request  enters  the interceptor  layer.  At  this  stage, middleware  can  inject  essential  metadata such  as  a  bearer  token  into  the  request headers  before  it  is  passed  to  the channel.  The  request  then  travels  across the  network  through  a  secure  TCP  or  TLS connection.  Once  it  reaches  the  server, it  passes  through  another  interceptor. This  serverside  middleware  validates  the incoming  token  and  can  append  additional data  to  the  request  extensions.  This enriched  request  is  then  handed  off  to the  core  service  logic  for  processing. Finally,  the  resulting  output  response is  sent  back  through  the  stack  to  the client,  completing  the  life  cycle.  Tonic provides  extensive  control  over  both network  security  and  data  serialization. In  terms  of  security,  it  supports granular  TLS  and  MTLS  configurations, allowing  for  a  full  mutual authentication  handshake  between  the client  and  server.  By  leveraging libraries  like  restless  or  native  TLS, you  can  manage  identities  and certificate  authorities  on  both  ends  of the  connection,  ensuring  robust encrypted  communication  with  support  for ALPNH2. Beyond  security,  Tonic  allows  for  deep customization  of  how  data  is  handled  in memory.  You  can  move  beyond  standard buffers  to  implement  custom  codecs  and buffer  limits  tailored  to  specific resource  constraints.  For  example,  the small  buffer  codec  shown  here demonstrates  how  to  enforce  strict memory  limits  such  as  a  512  byt  write limit  and  a  4,96  byt  read  limit  to prevent  resource  exhaustion. Furthermore,  the  framework's  flexibility extends  to  serialization  formats.  While protobuff  is  the  standard,  you  can integrate  alternative  codecs  like  JSON codec,  which  leverages  the  power  of certi.json  to  handle  JSON  payloads efficiently  within  your  gRPC  services. Tonic  natively  handles  protocol translation  for  gRPC  web  and  HTTP2 clearext,  also  known  as  H2C.  This  native support  is  a  significant  advantage  as  it removes  the  need  for  external  proxy infrastructure  such  as  Envoy, simplifying  your  overall  architecture. On  the  left  side  of  the  diagram,  we  see the  gRPC  web  layer.  When  a  browser  sends an  HTTP  1.1  request,  it  passes  through the  gRPC  web  layer.  Here,  Tonic  manages the  translation  of  the  one  byte  flag  and four  byte  length  prefix  while  also handling  the  necessary  base  64  encoding and  decoding.  This  allows  the  request  to be  consumed  directly  by  the  core  tonic service.  On  the  right  side,  we illustrate  the  H2C  upgrade  protocol. This  flow  begins  with  an  HTTP  1.1 request  that  includes  an  upgrade  header. The  server  switch  recognizes  this  header and  returns  a  switching  protocol  status. The  connection  is  then  handed  off  to  the serve  connection  with  upgrades  function which  transitions  the  communication  to  a full  HTTP2  stream.  By  handling  these protocols  internally,  Tonic  provides  a streamlined  path  for  web  clients  and clear  text  HTTP2  connections.  The  Tonic XDS  crate  enables  clients  to  adapt  to real-time  topology  changes  using  an aggregated  discovery  service  control plane.  This  dynamic  discovery  process  is structured  across  four  primary  layers that  process  information  from  the  top down.  At  the  top  of  the  hierarchy,  the XDS  control  plane  pushes  updates  to  the XDS  cache.  This  cache  data  then  informs each  subsequent  layer  of  the architecture  as  changes  occur  in  the environment.  In  layer  1,  the  listener works  with  the  XDS  router  to  inspect authority  and  path  headers.  This  initial stage  is  crucial  for  identifying  the intended  destination  of  a  request  within the  service  mesh.  Layer  2  focuses  on  the routing  phase.  Here,  the  XDSLB  service applies  specific  load  balancing  logic  to the  identified  route,  ensuring  traffic is  distributed  according  to  predefined policies.  Layer  3  manages  the  service cluster.  The  cluster  client  registry maintains  an  up-to-date  registry  of available  client  connections  for  a  given service  group,  bridging  the  gap  between routing  logic  and  physical  endpoints. Finally,  layer  4  handles  individual endpoints.  The  endpoint  manager  executes the  final  selection  process  using  the power  of  two  choices  or  P23  algorithm. This  ensures  that  the  most  optimal endpoint  is  selected  for  every  request, completing  the  end-to-end  dynamic discovery  and  routing  life  cycle. Utilizing  the  status  X-  trait,  Tonic facilitates  the  embedding  of  structured actionable  metadata  directly  within standard  gRPC  status  codes.  This hierarchical  construct  begins  with  a standard  tonic.st  status  such  as  an invalid  argument  error.  Wrapped  within this  status  is  a  prosttypes.in  payload which  serves  as  a  container  for comprehensive  error  details.  These details  can  include  specific  predefined objects  such  as  bad  request  for  field level  violations,  retry  info  for  delay parameters,  and  help  for  resource  URLs. To  consume  this  information,  the  client invokes  the  get  error  details  method, allowing  it  to  parse  these  nested structures  and  implement  sophisticated programmatic  error  handling  logic  based on  the  returned  metadata.  Operational observability  is  a  cornerstone  of productionready  systems  and  tonic provides  firstclass  support  through standardized  protocols  for  health checking  and  server  reflection.  These features  allow  infrastructure  components to  dynamically  monitor  and  interact  with services.  On  the  left  we  see  the implementation  of  health  checking  using the  tonic  health  crate.  A  health reporter  provides  updates  to  an  internal state  map  which  tracks  the  status  of various  service  components.  External clients  can  then  utilize  two  primary mechanisms.  A  unary  check  method  to retrieve  the  immediate  serving  status  or a  watch  stream  for  continuous  real-time updates  as  status  changes  occur.  On  the right,  the  tonic  reflection  crate enables  gRPC  server  reflection.  This process  begins  with  a  file  descriptor set  that  populates  the  reflection service  state.  By  exposing  this  metadata through  birectional  streams,  the  server allows  generic  tools  like  GRP  curl  to discover  and  query  service  methods dynamically,  facilitating  easier debugging  and  integration  without requiring  local  protocol  buffer definitions.  Tonic  utilizes  a  dedicated Terraform  provision  testing  architecture to  continuously  validate  its  prost  and protobuff  codecs  against  gRPC  go.  At  the infrastructure  layer,  Terraform  manages the  automated  provisioning  of  essential security  assets,  including  root  CAS  and server  certificates,  which  are maintained  within  a  dedicated  interrupt data  directory.  Within  the  orchestration layer,  a  comprehensive  test  script facilitates  direct  communication  between a  Rustbased  Tonic  client  and  a  gRPC  go server.  This  suite  evaluates  the  full range  of  gRPC  communication  patterns including  unary  calls,  client  streaming, server  streaming  and  birectional streaming.  By  conducting  these  rigorous tests,  we  can  confirm  exact  behavioral par  across  implementations,  guaranteeing seamless  interoperability  and maintaining  the  integrity  of  the protocol.  Our  comprehensive  diagnostic suites  utilize  rigorous  integration tests  to  ensure  absolute  stability.  We achieve  this  by  simulating  complex network  constraints,  compression negotiation,  and  various  code  generation edge  cases.  Starting  with  network limits,  we  focus  on  testing  HTTP2  keep alive  parameters  and  max  header  list size.  We  also  verify  that  oversized messages  correctly  yield  a  code.outrange error,  ensuring  the  system  handles boundary  conditions  gracefully.  In  the realm  of  load  shedding,  the  suites verify  how  the  system  behaves  under extreme  concurrency  and  resource starvation.  We  confirm  that  these scenarios  correctly  trigger code.resource  exhausted,  which  is  vital for  maintaining  service  integrity  under heavy  stress.  For  compression,  we validate  birectional  configurations  for zstad,  gzip,  and  deflate.  A  key  part  of this  process  is  ensuring  the  exact propagation  of  the  gRPC  encoding  header across  all  communication  channels. Finally,  regarding  stub  generation,  we validate  that  default  implementations correctly  yield  code  ununimplemented.  We contrast  this  with  explicitly  configured testing  stubs  designed  to  yield code.permission  denied,  allowing  for precise  control  and  verification  within our  testing  environments.  In-memory testing  constructs  provide  a  powerful method  for  verifying  system  behavior without  the  complexities  of  actual network  environments.  By  mocking  TCP streams  with  duplex  channels,  Tonic facilitates  rapid  isolated  validation  of complete  client  server  interactions, eliminating  the  latency  and  potential flakiness  associated  with  networking overhead.  Looking  at  the  architecture  on this  slide,  we  see  a  tonic  client  at node  A  which  is  configured  through tower.service  function.  This  client communicates  directly  with  a  tonic server  at  node  B  initiated  using  the serve  with  incoming  method.  The  bridge between  them  is  a  simulated  network layer  implemented  with  Tokyo.io.duplex providing  a  birectional  stream  for  async write  and  async  read  operations.  This approach  ensures  that  the  entire  request response  life  cycle  can  be  validated efficiently  and  reliably  within  a  single process.  The  tonic  framework  provides  a comprehensive  high-erformance architecture  that  unites  discrete abstractions  into  an  enterprise  ready microsurface  node.  Starting  from  the foundation,  the  Tokyo  runtime  manages the  underlying  asynchronous  IO  and  task scheduling,  providing  the  essential concurrency  required  for  high-scale environments.  Building  upon  this,  the network  core  layer  utilizes  XDAS  channel gRPC  and  specialized  server  components to  manage  complex  networking  and  request routing.  In  the  protocol  layer,  we utilize  the  prost  codeodc  for  efficient protobuff  serialization,  ensuring  the rapid  translation  of  data  across  the wire.  Moving  up  the  stack,  the middleware  layer  integrates  tower interceptors  and  gRPC  web  layer, enabling  developers  to  inject  modular logic  such  as  security,  telemetry,  and web  compatible  protocols  into  the request  life  cycle.  Finally,  the framework  is  rounded  out  with  integrated health  reporting  and  reflection endpoints.  These  components  provide  the essential  observability  and discoverability  required  for  modern distributed  systems,  allowing  for seamless  integration  and  monitoring within  a  production  microservices ecosystem.  them.

---
*Captured on May 2, 2026.*